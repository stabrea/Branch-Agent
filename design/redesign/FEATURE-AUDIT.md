# Feature audit: every action in Appendix A against the engine

Each of the 453 actions in BRANCH-DESIGN-INTENT.md Appendix A, checked against the engine at `redesign/window` (engine code as on trunk ecc91c16).

- **engine** (190): the engine does it; wire it to the route given.
- **window** (185): window-only; no engine needed.
- **soon** (26): the engine can't do it yet; drawn greyed out ("Coming soon").
- **proto** (10): prototype scaffolding; not built into the real window.
- **offscope** (42): the phone app or keepoak.com; not in this window.

`public/app/core/features.js` holds the verdicts as `ready` (engine or window) and `soon`. A control is clickable only once its builder has wired it (`markLive`); `ready` alone never makes it clickable, so nothing looks wired that isn't.
The "why" column says when a control is only partly backed (a variant the engine refuses, a switch that must be on, wording the engine can't honour). Wire only what the engine does.

| Action | Area | Verdict | Route | Evidence | Why |
|---|---|---|---|---|---|
| `dlg-close` | A.1 | window |  | prototype.html ACTS['dlg-close'] (closeDlg + toast by data-then) | Closing a dialog is window-only. The data-then follow-ups are where engine calls actually fire and the builder must wire them: removed = POST /api/trunks/{id}/remove (see remove); quit = Quit anyway (desktop quit, see quit); flagged = the per-reply report the engine lacks (see flag, soon); invited/signedin = invite link and sign-in flows (A.x outside this list). |
| `focus` | A.1 | window |  | prototype.html ACTS.focus -> toggleFocus() | Hides sidebar and status bar; local layout only. |
| `new-with` | A.1 | engine | POST /api/trunks/conversations {trunkId} | src/trunks/api.ts:46-49 conversationRoute -> trunks.startConversation; old window public/shell.js:538 startWithTrunk api("trunks/conversations", {trunkId}) (line numbers @a4abe51d) | Engine starts a new conversation answered by the chosen Trunk; today's window does exactly this (DG-093 chevron). |
| `new13-go` | A.1 | window |  | prototype.html: closes dialog and runs ACTS[data-a] | Dispatcher only; whatever data-a names carries its own state. |
| `newconv` | A.1 | engine | POST /api/run (no sessionId: the engine creates the conversation on the first message) | src/server.ts:1867 POST /api/run; old window public/app.js:2283 new-session handler clears sessionId, public/app.js:2202 api("run", {prompt, ...(sessionId?)}) (line numbers @a4abe51d) | Today's app does New conversation: the window clears the thread and the engine creates the session on first send (run.sessionId returned). |
| `newmenu` | A.1 | window |  | prototype.html POPS.newmenu | Popover of actions; each item is its own act. |
| `notes-toggle` | A.1 | proto |  | prototype.html ACTS['notes-toggle'] -> renderNotes() | Design-notes side panel is prototype annotation, not a product feature. |
| `notif-x` | A.1 | window |  | prototype.html ACTS['notif-x'] | Removes the banner element. |
| `palette` | A.1 | engine | GET /api/search?q= | src/server.ts:1320 GET /api/search -> unifiedSearch; old window public/unified-search.js:46 api(`search?q=`), palette public/shell.js:576 (line numbers @a4abe51d) | Ctrl K finder exists today and searches conversations, workflows and audit through the engine. First pass said soon; the route exists. Achievement unlock is window/delight. |
| `pane` | A.1 | window |  | prototype.html ACTS.pane | Sets which side pane is open. |
| `ph-go8` | A.1 | window |  | prototype.html ACTS['ph-go8'] -> phGo() | Phone-surface navigation; the screens it opens carry their own data. |
| `pip-x` | A.1 | window |  | prototype.html ACTS['pip-x'] | Closes the picture-in-picture view. |
| `places14` | A.1 | window |  | prototype.html ACTS.places14 (localStorage branch-proto-places14) | Local fold state. Rename the storage key off the 'branch-proto-' prefix in the real window. |
| `proto-reset` | A.1 | proto |  | prototype.html ACTS['proto-reset'] clears branch-proto-* localStorage | 'Start the prototype over' resets demo state; no meaning in the real app. |
| `ptabp` | A.1 | window |  | prototype.html ACTS.ptabp | Switches pane tab. |
| `quit` | A.1 | engine | POST /api/deployment/quit (Quit; the close button itself hides to the tray in the desktop shell, busy check GET /api/activity) | src/desktop/main.ts:191-194 window.on("close") hides; src/desktop/main.ts:479 asksBeforeQuit (src/desktop/quit-guard.ts); src/server.ts:3812 /api/deployment/quit handler (quitRequest, src/install/quit.ts); src/desktop/window-chrome-ipc.ts:5 close button drawn by the OS over the title row (line numbers @a4abe51d) | Engine/desktop already do exactly this (close to tray, confirm Quit while a task runs). Busy count: GET /api/activity. Not pure window: must go through the desktop shell, not a DOM toast. |
| `savefile` | A.1 | engine | POST /api/context-files {files:{[slot]:text}} | src/server.ts:1413 POST /api/context-files; old window public/context-files.js:198, public/agent-files.js:202 (line numbers @a4abe51d) | The file editor edits the files every Trunk reads first (SOUL.md/USER.md style); the engine saves them today. First pass said soon. |
| `side` | A.1 | window |  | prototype.html ACTS.side | Mobile sidebar class toggle. |
| `side-toggle` | A.1 | window |  | prototype.html ACTS['side-toggle'] | Sidebar visibility + saved layout. |
| `surface` | A.1 | window |  | prototype.html ACTS.surface | Switches which surface is drawn. |
| `surfpop` | A.1 | window |  | prototype.html ACTS.surfpop | Opens the surface popover. |
| `switchperson` | A.1 | engine | POST /api/profiles/switch {profileId\|null, pin?} | src/collab-server.ts:211-220 profiles.switch + audit; old window public/people-place.js:46, public/collab.js:244,256 (line numbers @a4abe51d) | Switching who uses the computer is an engine profile switch (it changes whose records are reachable). First pass said window. |
| `switchto` | A.1 | engine | POST /api/profiles/switch {profileId, pin} | src/collab-server.ts:211 -> src/profiles.ts:103 switch(); owner PIN set via POST /api/profiles/owner-pin src/collab-server.ts:223; old window public/collab.js:256 sends the owner PIN (line numbers @a4abe51d) | The PIN for switching back to the owner is enforced by the engine. First pass said soon. |
| `tasks10` | A.1 | engine | GET /api/activity | src/server.ts:1619 GET /api/activity; old window public/shell.js:450, public/app.js watchActivity (line numbers @a4abe51d) | The popover is a readout of working tasks; the engine lists them. |
| `updmenu` | A.1 | engine | POST /api/comfort/update-plan (install through the desktop updater) | src/comfort/api.ts:157 update-plan (busyTasks, channel, autoUpdate); old window public/app.js:1174; src/desktop/updater-ipc.ts (line numbers @a4abe51d) | The popover shows whether an update is ready and installs when idle; both are engine/desktop data. |
| `usagepop` | A.1 | engine | GET /api/usage/glance | src/server.ts:2013 usageGlancePath -> usageGlance (src/usage-limits-api.ts:136); old window public/usage-glance.js:203 (line numbers @a4abe51d) | Account usage windows come from the usage glance route. First pass cited GET /api/comfort, which is the comfort settings card. |
| `view` | A.1 | window |  | prototype.html ACTS.view | Switches the main view. |
| `chat` | A.2 | engine | GET /api/sessions/{id} | src/server.ts:2069 sessionApi GET -> store.sessionView; old window public/app.js:1880,1896 api("sessions/" + id) (line numbers @a4abe51d) | Opening a conversation reads it from the engine today. Note: newmenu's 'New Trunk' uses chat with data-id="new" = the new-Trunk flow, POST /api/trunks (src/trunks/api.ts:93). |
| `closepop` | A.2 | window |  | prototype.html ACTS.closepop | 'Remind me tomorrow' only closes and snoozes locally. |
| `fileopen` | A.2 | engine | GET /api/artifacts/file?path= (+ GET /api/history/files?path= for earlier versions) | src/server.ts:4117 GET /api/artifacts/file; src/server.ts:2119 GET /api/history/files; states from GET /api/runs/{id}/receipts src/server.ts:1605; old window public/panels.js:98 (line numbers @a4abe51d) | Engine serves the file and its before/after history. Caveat: no single 'files of this conversation' route; the window builds the Read/Changed/Made list from run receipts. |
| `find-close` | A.2 | window |  | prototype.html ACTS['find-close'] | In-thread find state. |
| `find-open` | A.2 | window |  | prototype.html ACTS['find-open'] | In-thread find state. |
| `find-step` | A.2 | window |  | prototype.html ACTS['find-step'] | Moves highlight between local hits. |
| `forget` | A.2 | engine | POST /api/action {tool:"memory.delete", args:{id}} | src/server.ts:1898 POST /api/action; src/memory.ts:562 memory.delete; old window public/app.js:368; Undo = POST /api/memory/versions/restore src/server.ts:2242 (src/memory-review.ts:265-273 brings back a deleted fact from its last kept version) (line numbers @a4abe51d) | First pass cited /api/memory/forget/preview, which forgets what one conversation taught (public/app.js:1826), not one memory row. |
| `sess-carry` | A.2 | engine | POST /api/sessions/{id}/duplicate | src/server.ts:2102 duplicate -> store.duplicateSession; old window public/app.js:1940 (line numbers @a4abe51d) | A new conversation carrying everything from the earlier one. First pass said soon. |
| `sq-clear` | A.2 | window |  | prototype.html ACTS['sq-clear'] | Clears the search box. |
| `sq-f` | A.2 | window |  | prototype.html ACTS['sq-f'] | Filter chip over results already fetched. |
| `sr-msg` | A.2 | engine | GET /api/search?q= / POST /api/sessions/search, then GET /api/sessions/{id} | src/server.ts:1320, src/server.ts:2028, src/server.ts:2069; old window public/app.js:1920 api("sessions/search") (line numbers @a4abe51d) | Message hits and opening their conversation are engine reads. First pass said soon. |
| `sr-sess` | A.2 | engine | GET /api/sessions/{id} | src/server.ts:2069; old window public/app.js:1896 (line numbers @a4abe51d) | A past session is an earlier conversation; the engine returns it with its messages. |
| `artbig` | A.3 | window |  | prototype.html ACTS.artbig | Redraws a chart already in the thread, larger. |
| `beside15` | A.3 | window |  | prototype.html ACTS.beside15 | Split view; the second conversation is read with GET /api/sessions/{id} like chat. |
| `bg-new` | A.3 | window |  | prototype.html ACTS['bg-new'] | Pre-fills '/bg ' in the draft; sending it goes through bgrun15's route. |
| `bglist15` | A.3 | engine | GET /api/activity | src/server.ts:1619; /bg runs are ordinary runs in their own conversation (src/autonomy/commands.ts:72-90) (line numbers @a4abe51d) | Readout of running tasks. Caveat: the engine keeps the /bg set only in memory (src/autonomy/commands.ts:69), so the list is every working task, not only /bg ones. |
| `bgopen15` | A.3 | engine | GET /api/sessions/{id} | src/server.ts:2069 (line numbers @a4abe51d) | Opens the task's conversation. |
| `bgrun15` | A.3 | engine | POST /api/commands/run {surface:"window", line:"/bg <draft>", sessionId} | src/commands/api.ts:77; handler src/autonomy/commands.ts:72-90 (at most 3, maxBackground); catalog src/commands/catalog.ts:116; old window public/commands.js:75 (line numbers @a4abe51d) | Engine runs /bg. Needs the autonomy 'session-commands' part switched on. |
| `bgstop15` | A.3 | engine | POST /api/runs/{runId}/cancel | src/server.ts:1563 runs/(cancel\|...); old window public/live-run.js:68, public/app.js:683 (line numbers @a4abe51d) | Stopping a task is a real route. |
| `call` | A.3 | engine | POST /api/voice/live (then the WebSocket it returns) | src/voice-api.ts:56 openLive (dispatch src/server.ts:1483); old window public/voice-live.js:132 (line numbers @a4abe51d) | Talk live exists. First pass said voice needs VoIP; it is a local live-voice socket, owner only. |
| `call-end` | A.3 | engine | POST /api/voice/live (end: {live:"stop"} on the socket it opened) | public/voice-live.js:193-194 end(); spoken lines join the conversation (public/app.js branchAddSpokenMessage) (line numbers @a4abe51d) | Ending the live conversation is the engine's stop message. Transcript wording: lines go into the conversation, not a 'Files' transcript. |
| `chatmenu` | A.3 | window |  | prototype.html POPS.chatmenu | Menu of other acts. |
| `ckpt` | A.3 | engine | POST /api/sessions/{id}/rewind {messageId, restore: files} (in a git folder: POST /api/marks/undo {id}) | src/sandbox-remote-api.ts:104 marks/undo -> GitCheckpoints (src/git-checkpoint.ts, made before a change set is written); alternatives POST /api/sessions/{id}/rewind with files (src/rewind.ts:208) and per-file POST /api/history/restore src/server.ts:2121 (old window public/app.js:224) (line numbers @a4abe51d) | Putting files back is real. First pass cited POST /api/memory/checkpoints, which saves memories and skill versions only (public/app.js:301,319). Default to POST /api/sessions/{id}/rewind with restore of files (works outside git when rewind snapshots are on); /api/marks/undo only when the folder is a git repository. |
| `compare` | A.3 | engine | GET /api/runs/{id}/inspect (for each of the two tasks) | src/server.ts:1951 inspectMatch; old window public/compare.js:122 (line numbers @a4abe51d) | Today's app compares two tasks side by side from inspect data. First pass said soon. |
| `dict` | A.3 | engine | POST /api/voice/dictation/listen {on:true} | src/server.ts:1284-1298 voice/dictation(+/listen); old window public/dictation.js:162 (line numbers @a4abe51d) | Starting dictation opens the microphone through the engine; not window-only. |
| `dict-done` | A.3 | engine | POST /api/voice/dictation/listen {on:false} + GET /api/voice/dictation (words) | src/server.ts:1284-1298; old window public/dictation.js:140,162 (line numbers @a4abe51d) | Dictation exists; the words come from the engine. |
| `flag` | A.3 | soon |  | src/diagnostic-api.ts:162 /api/diagnostics/report/issue only builds an issue link from activity-log items; public/activity-log.js:87 'Report a problem' is app-wide (line numbers @a4abe51d) | Missing: a per-reply report (this reply + reason + note) with somewhere to send it. Searched src/ for flag/feedback/thumbs/report-a-reply: nothing per message. |
| `imagine` | A.3 | window |  | prototype.html ACTS.imagine opens the 'Make a picture' dialog | Dialog opener. Not proto: the engine can make pictures (see img-go). |
| `img-again` | A.3 | engine | POST /api/run {sessionId, prompt} (media.image again) | src/media.ts:295 media.image; src/server.ts:1867 (line numbers @a4abe51d) | Regenerate = ask again. Same provider requirement as img-go. |
| `img-bg` | A.3 | window |  | public/delight-background.js:1-4 (own background kept in the window's IndexedDB, never sent to the server) | Setting a picture as the background is window-side today; the picture bytes come from the engine's artifact. |
| `img-go` | A.3 | engine | POST /api/run {sessionId, prompt:"Make a picture: ..."} -> media.image | src/media.ts:294-300 media.image ('Make a picture from a description'); src/server.ts:1867 (line numbers @a4abe51d) | Engine makes pictures. Caveats: needs a connected provider that makes pictures (src/media.ts:128 providerImages; src/media-settings.ts:6); one call makes one picture, so the prototype's 4 variants means 4 calls or one picture. |
| `img-pick` | A.3 | window |  | prototype.html ACTS['img-pick'] | Chooses among pictures already made. |
| `insert` | A.3 | window |  | prototype.html ACTS.insert | Draft edit. |
| `inspect` | A.3 | engine | GET /api/runs/{runId}/inspect | src/server.ts:1951; old window public/inspector.js:128 (line numbers @a4abe51d) | First pass cited GET /api/look, which is the terminal theme route (src/server.ts:1466 -> src/terminal-theme.ts lookApi). |
| `jit16` | A.3 | window |  | prototype.html ACTS.jit16 | Hides the in-chat permission card for this session. |
| `matrm15` | A.3 | window |  | prototype.html ACTS.matrm15 | Draft edit. |
| `mention-pick` | A.3 | window |  | prototype.html ACTS['mention-pick'] | Draft edit; the Trunk names come from GET /api/trunks. |
| `mplay15` | A.3 | window |  | old window public/playback.js inline playback (public/media.js:129); file from GET /api/attachments/file (public/app.js:1597) | Play/pause of a real file is browser-side media playback. First pass said soon. |
| `mseek15` | A.3 | window |  | old window public/playback.js inline playback | Seeking is browser-side media playback. First pass said soon. |
| `offgo15` | A.3 | engine | POST /api/run {sessionId, prompt} -> documents.write | src/document-authoring.ts:139-142 documents.write writes .docx/.xlsx/.pptx; src/server.ts:1867 (line numbers @a4abe51d) | Engine writes Office files. First pass said Office integration missing. |
| `office15` | A.3 | window |  | prototype.html ACTS.office15 | Opens the 'Write a file' dialog. |
| `offk15` | A.3 | window |  | prototype.html ACTS.offk15 | Picks the kind in the dialog. |
| `pausetrunk` | A.3 | soon |  | src/trunks/record.ts:55-74 TrunkSchema has hidden/pinned/order but no paused; src/trunks/api.ts:95 only /api/trunks/switch (whole feature off) (line numbers @a4abe51d) | Missing: a per-Trunk pause. First pass marked live on POST /api/trunks, which creates a Trunk. |
| `ph-voice` | A.3 | engine | POST /api/voice/transcribe + POST /api/voice/speak (turn by turn) | src/server.ts:4191 voice/transcribe; src/short-lived-keys.ts:63-66 a phone key may transcribe and speak but not /api/voice/live (line numbers @a4abe51d) | Phone voice works turn by turn. Caveat: a live call is owner-window only. |
| `pick` | A.3 | engine | POST /api/run {sessionId, prompt: the option's words} | src/index.ts:592-594 user.ask: 'their next message in this conversation is the answer'; src/server.ts:1867 (line numbers @a4abe51d) | Picking = sending that answer. Caveat: the engine's questions carry no option list (user.ask, ask-first give only text), so choice blocks appear only where the window draws them. |
| `pin` | A.3 | engine | POST /api/trunks/{id} {pinned} (rooms: POST /api/trunks/rooms/{id} {pinned}) | src/trunks/api.ts:124 edit, src/trunks/record.ts:72 pinned; src/trunks/api.ts:159 rooms.edit, src/trunks/rooms.ts:36; old window public/strip.js:275,330; plain conversations pinned locally public/shell.js:378,389 (line numbers @a4abe51d) | Today's app pins Trunks in the engine and plain conversations in window storage. First pass said soon. |
| `pin-id` | A.3 | engine | POST /api/trunks/{id} {pinned} (rooms: POST /api/trunks/rooms/{id} {pinned}) | same as pin; public/shell.js:389 togglePin for plain conversations (line numbers @a4abe51d) | Same as pin, by id. |
| `pin15` | A.3 | engine | POST /api/sessions/{id}/pins {messageId, pinned} | src/server.ts:2045-2051 pins -> store.pinMessage (src/store.ts:488); pinned messages kept through folding (src/store.ts:465-473) (line numbers @a4abe51d) | The engine pins messages so they stay in front of the assistant, exactly the row's promise. First pass said window. |
| `pinjump15` | A.3 | window |  | prototype.html ACTS.pinjump15 | Scrolls to a message. |
| `pinlist15` | A.3 | engine | GET /api/sessions/{id}/pins | src/server.ts:2046 (line numbers @a4abe51d) | Readout of the engine's pins. |
| `plusmenu` | A.3 | window |  | prototype.html POPS.plusmenu | Menu. Its toast items (Attach files, Add a folder, screenshot) are placeholders to wire or grey. |
| `project` | A.3 | engine | GET /api/projects + POST /api/projects/active {active} | src/owner-data-api.ts:12,19; old window public/shell.js:287 (line numbers @a4abe51d) | Projects are engine data. First pass said soon. |
| `projtoggle` | A.3 | window |  | prototype.html ACTS.projtoggle | Fold state. |
| `prompt-use` | A.3 | engine | GET /api/prompts | src/prompt-library-api.ts:86; old window public/prompt-library.js:299 (line numbers @a4abe51d) | Saved prompts are stored by the engine; Use loads one into the draft. First pass said soon. |
| `prompts-fill` | A.3 | window |  | prototype.html ACTS['prompts-fill'] | Draft edit. |
| `qrm15` | A.3 | engine | POST /api/flows-boards/waiting/followups/remove {sessionId, id} | src/flows-boards/api.ts:117-123; old window public/flows-boards.js:261 (line numbers @a4abe51d) | Queued follow-ups live in the engine. Needs the 'waiting-line' part on. First pass said window. |
| `queue15` | A.3 | engine | GET /api/sessions/{id}/followups (or GET /api/flows-boards/waiting?session=) | src/server.ts:2060-2063; src/flows-boards/api.ts:105-109 (line numbers @a4abe51d) | Readout of the engine's waiting line. |
| `qup15` | A.3 | engine | POST /api/flows-boards/waiting/followups/move {sessionId, id, direction:"up"} | src/flows-boards/api.ts:117-123; old window public/flows-boards.js:260 (line numbers @a4abe51d) | Reordering is an engine change. Needs 'waiting-line' on. First pass said window. |
| `remove` | A.3 | engine | POST /api/trunks/{id}/remove | src/trunks/api.ts:129 -> src/trunks/index.ts:256-268 (line numbers @a4abe51d) | Real. Wording caveat: the engine has no 30-day archive; it stops the Trunk's routines and hands its conversations back to your assistant. The dialog's confirm (dlg-close data-then=removed) is where the call fires. |
| `rename` | A.3 | engine | POST /api/trunks/{id} {name} (rooms: POST /api/trunks/rooms/{id} {name}) | src/trunks/api.ts:124,159; old window public/studio.js:466; plain conversations named locally public/shell.js:377,381 (line numbers @a4abe51d) | Today's app renames Trunks in the engine and plain conversations in window storage. First pass said soon. |
| `rename-id` | A.3 | engine | POST /api/trunks/{id} {name} (rooms: POST /api/trunks/rooms/{id} {name}) | same as rename (line numbers @a4abe51d) | Same as rename, by id. |
| `roommenu` | A.3 | engine | GET /api/sessions/{id}/context | src/server.ts:2039-2042 tokenReport; tidy-up = /compact via POST /api/commands/run (src/commands/catalog.ts:98) (line numbers @a4abe51d) | Room-left readout is engine data. |
| `roster10` | A.3 | engine | GET /api/trunks + GET /api/reach/trunks/roster | src/trunks/api.ts:91 overview; src/reach/api.ts:63 (line numbers @a4abe51d) | Readout of the Trunks it knows. The per-row 'may talk to' switches are a separate data-sw, not in this list. |
| `roster10h` | A.3 | engine | GET /api/trunks + GET /api/reach/trunks/roster | src/trunks/api.ts:91; src/reach/api.ts:63 (line numbers @a4abe51d) | Same roster, from the header. |
| `rw-go` | A.3 | engine | POST /api/sessions/{id}/rewind {messageId, restore}, then POST /api/run | src/rewind.ts:208 (dispatch src/server.ts:1358); undo POST /api/sessions/{id}/unrevert; old window public/rewind.js:118 (line numbers @a4abe51d) | Rewind conversation/files/both exists. First pass said soon. |
| `rw-what` | A.3 | window |  | prototype.html ACTS['rw-what'] | Choice inside the rewind dialog. |
| `seg` | A.3 | window |  | prototype.html ACTS.seg | Pressed state of a segmented control. |
| `slash-pick` | A.3 | window |  | prototype.html ACTS['slash-pick'] | Draft edit; list from GET /api/commands?surface=window. |
| `slash6-pick` | A.3 | window |  | prototype.html ACTS['slash6-pick'] | Draft edit. |
| `spendmenu` | A.3 | engine | GET /api/usage?range=&by= (+ GET /api/sessions/{id}/cost) | src/server.ts:1905; src/server.ts:2044 (line numbers @a4abe51d) | Spend readout is engine data. |
| `sugg` | A.3 | engine | POST /api/run {sessionId, prompt} | src/server.ts:1867 (line numbers @a4abe51d) | Sets the draft and sends it; sending is the engine. First pass said window. |
| `toast` | A.3 | window |  | prototype.html ACTS.toast | Generic toast. Every prototype button using data-act=toast is a placeholder (Attach files, Tidy up, Put it back, Copy the record...): the builder must give each a real act or grey it. |
| `u-edit` | A.3 | window |  | prototype.html ACTS['u-edit']; old window public/rewind.js:72 reads GET /api/sessions/{id}/rewind first | Opens the rewind dialog; the change itself is rw-go. Should read rewind status to show what can be taken back. |
| `undo` | A.3 | window |  | prototype.html ACTS.undo | Calls the toast's own undo callback. |
| `voice` | A.3 | engine | POST /api/voice/live | src/voice-api.ts:56; old window public/voice-live.js:132 (line numbers @a4abe51d) | Talk live exists. First pass said soon. |
| `wake16` | A.3 | engine | POST /api/voice/wake | src/server.ts:1264-1272 voice/wake; old window public/wake-word.js:52,58 (line numbers @a4abe51d) | Wake word is an engine setting. First pass said soon. |
| `allowall` | A.4 | engine | GET /api/policy (waiting list) | src/server.ts:1816-1817 returns waiting: app.runtime.approvals.waiting() (line numbers @a4abe51d) | Opens a confirm showing the real pending count. First pass cited POST /api/asks, which is bucket 23 (pages, analytics, leads), not approvals (src/asks/api.ts:10). |
| `allowall-go` | A.4 | engine | POST /api/policy/approve {sessionId, decision:"allow", remember:"never", fingerprint, carryOn:true} per waiting question | src/server.ts:1823-1835; old window public/approvals.js:132 (line numbers @a4abe51d) | No batch route; one call per waiting question does it. |
| `ask` | A.4 | engine | POST /api/policy/approve {sessionId, decision, remember, fingerprint, carryOn:true} | src/server.ts:1823; old window public/approvals.js:130-140, public/live-run.js:18 (line numbers @a4abe51d) | Send it = allow/never, Always allow = allow/always, Don't send = deny. Not /api/asks. |
| `ckpt-demo` | A.4 | proto |  | prototype.html ACTS['ckpt-demo'] -> checkpointPrompt(true) with made-up numbers | Demo trigger for the save-progress bar; the real setting is the glance-save-progress switch. |
| `ckpt-no` | A.4 | window |  | prototype.html ACTS['ckpt-no']; old window public/usage-glance.js:15 remembers it asked | Dismisses the offer. |
| `ckpt-save` | A.4 | engine | POST /api/usage/save-progress | src/usage-limits-api.ts:150-154 saveProgress; old window public/usage-glance.js:155 (line numbers @a4abe51d) | Asks running tasks to save progress near an account limit. First pass cited POST /api/memory/checkpoints (memories). |
| `g-all` | A.4 | engine | POST /api/policy/approve {decision:"allow"} per pending item | src/server.ts:1823 (line numbers @a4abe51d) | Grouped approvals are the same approvals. Not /api/asks. |
| `g-ans` | A.4 | engine | POST /api/policy/approve {sessionId, decision, remember, fingerprint} | src/server.ts:1823 (line numbers @a4abe51d) | One item of a grouped approval. Not /api/asks. |
| `lock` | A.4 | engine | POST /api/lockdown {on} | src/other-api.ts:38 lockdownApi; old window public/conversation-mode.js:137 (line numbers @a4abe51d) | Lockdown is an engine switch. First pass said window. |
| `lockscreen` | A.4 | engine | POST /api/lock (unlock: POST /api/lock/unlock) | src/server.ts:1368 -> guardApi src/server.ts:2260-2261 sessionLock; old window public/app.js:2049 (line numbers @a4abe51d) | Locking Branch is an engine session lock. First pass said window. |
| `mode` | A.4 | engine | POST /api/conversation-mode {sessionId, mode} (clearing Lockdown: POST /api/lockdown {on:false}) | src/conversation-mode-api.ts:17-18 (dispatch src/server.ts:1895); modes ask/plan/auto/full src/conversation-mode.ts:34; old window public/conversation-mode.js:196 (line numbers @a4abe51d) | Today's mode chip does this. Label map: Ask first -> ask, Just do it -> auto; 'Ask for everything' has no exact engine mode (nearest plan). First pass said soon. |
| `model` | A.4 | engine | POST /api/models/switch {sessionId, model} | src/voice-api.ts:64 (dispatch src/server.ts:1485); old window public/app.js:2132; per-conversation preset POST /api/sessions/{id}/model src/server.ts:2085 (line numbers @a4abe51d) | Model choice per conversation exists. First pass said soon. |
| `modelmenu` | A.4 | window |  | prototype.html POPS.modelmenu | Menu; its choices come from GET /api/models/switch (src/voice-api.ts:63), the pick is model. |
| `modelmenu2` | A.4 | window |  | prototype.html POPS.modelmenu2 | Menu; choices from GET /api/models/switch, thinking pick is think. |
| `modemenu` | A.4 | window |  | prototype.html POPS.modemenu | Menu; choices from GET /api/conversation-mode?sessionId=. |
| `modemenu2` | A.4 | window |  | prototype.html POPS.modemenu2 | Menu; choices and blocked reasons from GET /api/conversation-mode?sessionId=. |
| `pauseall` | A.4 | soon |  | no pause in src/trunks/*; POST /api/dashboard/automations pauses schedules/triggers only; POST /api/safety-extras/stop (src/safety-extras/api.ts:118, emergency-stop.ts) refuses every tool and its release may need a code (line numbers @a4abe51d) | Missing: pause/resume for all Trunks. First pass marked live on POST /api/trunks, which creates a Trunk. |
| `ph-allowall` | A.4 | engine | POST /api/policy/approve {decision:"allow"} per waiting question | src/server.ts:1823; short-lived keys may answer once/this conversation (src/server.ts:1833-1835) (line numbers @a4abe51d) | Same as allowall-go from the phone. Not /api/asks. |
| `rec` | A.4 | engine | GET /api/deployment/suggestion; yes: POST /api/deployment/daemon {action:"install"} or POST /api/comfort {card:"notify", values:{autoUpdate:"install"}}; never: POST /api/deployment/suggestion {id, answer:"never"} | src/deployment-api.ts:172,174,183; old window public/suggestions.js:15,20,68,82 (line numbers @a4abe51d) | The recommendation bar exists today (background engine and auto-update). 'Not now' is window-only. First pass said window. |
| `scope` | A.4 | window |  | prototype.html ACTS.scope | Chooses which route set-mode uses (here vs everywhere). |
| `self-apply` | A.4 | engine | POST /api/never-break/proposal/accept | src/never-break/api.ts:68-70 (dispatch src/server.ts:1195); assistant side proposes and tries it on a throwaway copy src/never-break/api.ts:100-102 (line numbers @a4abe51d) | Real. Wording caveat: the engine applies it at the next start, not 'live'. First pass said soon. |
| `self-no` | A.4 | engine | POST /api/never-break/proposal/discard | src/never-break/api.ts:72 (line numbers @a4abe51d) | Discards the proposal. First pass said window. |
| `self-undo` | A.4 | engine | POST /api/never-break/rollback (the last accepted change, in GET /api/never-break `accepted`) | src/never-break/gateway-config.ts rollbackAccepted, journal gateway.changes.json; src/never-break/api.ts | Accepting a suggested change now writes its timings before and after to a journal beside gateway.json; Roll back puts the timings back (switch and engine settings stay as they are), once, and is refused when the timings changed since. Takes effect at the next start, as accepting does. |
| `set-mode` | A.4 | engine | POST /api/conversation-mode {sessionId, mode} (here); POST /api/conversation-mode/settings {newConversation} (everywhere) | src/conversation-mode-api.ts:17-18,90; old window public/conversation-mode.js:196,238 (line numbers @a4abe51d) | PMODES auto/ask/plan/none map to engine auto/ask/plan/full. 'Everywhere' = the new-conversation default; conversations with no mode of their own follow the owner's policy preset (POST /api/policy). First pass said soon. |
| `suggest` | A.4 | engine | POST /api/autonomy/suggestions {fingerprint, yes} | src/autonomy/api.ts:71-75; old window public/autonomy.js:94-95 (line numbers @a4abe51d) | Make it / Not interested exist. First pass said soon. |
| `think` | A.4 | engine | POST /api/sessions/{id}/model {reasoning: low\|medium\|high} | src/server.ts:2085; values src/models.ts:11; old window public/app.js:1415 (line numbers @a4abe51d) | Quick/Normal/Deep map to low/medium/high. First pass said window. |
| `v-ans` | A.4 | engine | POST /api/personal/voice/answer (a tap: POST /api/policy/approve) | src/personal/api.ts:90 (line numbers @a4abe51d) | Voice approvals exist; needs the personal part 'voice-approvals' on (src/personal/voice-approvals.ts:73). First pass said soon. |
| `v-end` | A.4 | engine | POST /api/voice/live (end: {live:"stop"} on the socket it opened) | public/voice-live.js:193-194 (line numbers @a4abe51d) | Same as call-end. |
| `v-mute` | A.4 | window |  | public/voice-live.js:77,236 muted flag stops sending microphone audio | Mute is window-side. |
| `who` | A.4 | engine | POST /api/trunks/conversations/{sessionId} {trunkId\|null} | src/trunks/api.ts:51-57 conversations.choose; old window public/app.js:2240 reads trunks/conversations/{id} (line numbers @a4abe51d) | Who answers in an ordinary conversation is engine state. Caveat: rooms ('Whoever fits' or a named member) are not covered; RoomEditSchema (src/trunks/rooms.ts:31-38) has no who-answers field. First pass said soon. |
| `xdo` | A.4 | engine | POST /api/flows-boards/installs/{id}/approve \| /decline | src/flows-boards/api.ts:132-140; request made by install.request src/flows-boards/tools.ts:96; old window public/flows-boards.js:365 (line numbers @a4abe51d) | Not /api/asks. Caveats: needs the 'install-requests' part on; approving does not install anything (the answer says what to run), so the 'installed' toast must change. |
| `br-take` | A.5 | engine | POST /api/linux-desktop/take-over; POST /api/linux-desktop/hand-back | @0cc2b299 src/server.ts:1582; src/server.ts:1587; src/integrations/linux-desktop-tools.ts:10; public/linux-desktop.js:80 | The engine's owner take-over and hand-back of the computer Branch is driving (the shared Linux desktop): while the owner holds it every desktop tool is refused, and only the owner can hand it back. Wire Take over / Hand back to these two routes and show the button only while GET /api/linux-desktop reports a running desktop (control 'agent' shows Take over, 'user' shows Hand back); on any other browser pane it does nothing. Ships switched off (Settings > Computer & browser). Branch's own headless browser has no live hand-over; the only one there is the 'site asked if you are a person' hand-over (src/web-pages.ts handOver), which the owner answers from the app. |
| `comp-grid` | A.5 | window |  |  | Shows every computer's screen in a grid; view state only (S.stageGrid). Screens come from run events already streamed. |
| `comp-view` | A.5 | window |  |  | Picks which of the Trunk's computers the stage shows (prototype sets S.compFor for the view, not an assignment). View state only. |
| `machine` | A.5 | engine | POST /api/reach/machines/start; POST /api/reach/machines/look | @0cc2b299 src/reach/api.ts:86; src/reach/api.ts:85; src/reach/machines.ts:8; public/reach.js:127 | The engine drives other computers running Branch side by side: it lists them (GET /api/reach/machines), reads their conversations and tasks (look) and starts a task there (start); the key is filled in by this computer. Talking to the assistant on another machine goes through these. Off unless the owner added computers under 'Other computers running Branch' (bucket 23 'nodes' switch). |
| `machines` | A.5 | window |  |  | Opens the machine switcher menu. Its list is GET /api/reach/machines plus this computer's name, already loaded; opening it is window-only. |
| `ph-comp` | A.5 | window |  |  | Phone navigation to a Trunk's computer view (S.ph.chat, phGo). Window-only. |
| `ph-stop8` | A.5 | engine | POST /api/runs/{id}/cancel | @0cc2b299 src/server.ts:1601; src/server.ts:1607; public/app.js:231 | stopWork() is stopping the task: POST /api/runs/{id}/cancel stops a working task, and a waiting or interrupted one too. The first pass cited POST /api/trunks/{id}/stop, which does not exist (src/trunks/api.ts has no such action). |
| `rc-save` | A.5 | engine | POST /api/devices/{id}/rename; POST /api/asks/nodes; POST /api/reach/machine-name | @0cc2b299 src/devices/api.ts:88; src/asks/api.ts:99; src/reach/api.ts:84; public/reach.js:116 | Every kind of machine in the switcher can be renamed by the engine: a paired device (devices rename), another Branch computer (save the nodes list with the new name) and this computer (reach machine-name). Not window-only: the name must persist. |
| `renamecomp` | A.5 | window |  |  | Opens the rename dialog pre-filled with the name already on screen. Window-only; the save is rc-save. |
| `stage` | A.5 | window |  |  | Chooses what the stage shows (computer or browser) and for which conversation; view state only. |
| `stage-close` | A.5 | window |  |  | Closes the stage view; view state only. |
| `stage-dock` | A.5 | window |  |  | Toggles the side panel; view state only. |
| `stage-pip` | A.5 | window |  |  | Moves the stage to picture-in-picture; view state only. |
| `stage-step` | A.5 | engine | GET /api/runs/{id}/recording | @0cc2b299 src/run-recording-api.ts:6; src/run-recording-api.ts:81; src/run-recording-api.ts:85; src/run-recording.ts:28; public/recordings.js:106 | Showing the screen as it was at an earlier step needs the task's recorded frames, which come from the engine's run recording (frames per step, with the pictures the task looked at). 'live' is just the current stream. Recordings are behind their own switch (requireRecordings refuses while it is off), and pictures are only kept while 'pictures in recordings' is also on; otherwise a step shows its words only. |
| `stage-stop` | A.5 | engine | POST /api/runs/{id}/cancel | @0cc2b299 src/server.ts:1601; src/server.ts:1607; public/app.js:231 | stopWork() stops the running task: POST /api/runs/{id}/cancel. The first pass's POST /api/trunks/{id}/stop does not exist. |
| `takeover` | A.5 | engine | POST /api/linux-desktop/take-over | @0cc2b299 src/server.ts:1582; src/server.ts:1587; src/integrations/linux-desktop-tools.ts:10; public/linux-desktop.js:80 | 'You have the mouse. Scout is paused.' is the engine's owner take-over: Branch lets go of the shared desktop and every desktop tool is refused until the owner hands it back (hand-back route). Ships switched off; show the button only while GET /api/linux-desktop says Branch is using a running desktop. |
| `bmove15` | A.6 | window |  |  | Opens the 'Move to' popover; window-only. The move itself is bto15. |
| `bto15` | A.6 | engine | POST /api/flows-boards/board/cards/{id}/move | @0cc2b299 src/flows-boards/api.ts:86; src/flows-boards/kanban.ts:25; public/flows-boards.js:186 | The engine's board moves a card between its five lanes (todo, doing, review, done, blocked) with { lane }; the design's five columns map onto them. Behind the flows-boards switch. |
| `cutgo15` | A.6 | engine | POST /api/runs/{id}/resume | @0cc2b299 src/server.ts:1617; src/runtime.ts:676; src/runtime.ts:1318; src/store.ts:418; public/app.js:239 | A task cut off because Branch closed (an update's restart) is kept as 'interrupted' and the engine continues it from its saved transcript; the inbox card's list is store.waitingRuns. The first pass said 'needs backend'; it exists. |
| `cutno15` | A.6 | engine | POST /api/runs/{id}/cancel | @0cc2b299 src/server.ts:1607; src/server.ts:1601 | Leaving it must also end the interrupted task, or the card comes back on the next load (waitingRuns keeps listing it). The engine's cancel route stops an interrupted task and drops its question. |
| `dv15` | A.6 | engine | POST /api/knowledge/graph | @0cc2b299 src/knowledge-more.ts:131; public/docs-3.js:70 | List is the already-loaded documents; Map draws the engine's knowledge graph. The engine answers a neighbourhood around one entity (collection, entity, depth), not a whole-library map in one call, so the Map starts from an entity. |
| `idea15` | A.6 | window |  |  | Fills the Scheduled describe box with one of the design's idea texts; nothing is saved until Add. Window-only. |
| `ideas15` | A.6 | window |  |  | Opens the fixed list of 13 idea texts from the design catalogue. Window-only. |
| `mem` | A.6 | engine | POST /api/memory/proposals/{id}/accept; POST /api/memory/proposals/{id}/reject | @0cc2b299 src/server.ts:2271; public/app.js:290 | 'It asks before it remembers': a suggested fact is a review proposal; Keep accepts it into memory, Forget rejects it. The first pass cited GET /api/memory/export, which is the wrong route. |
| `memarch15` | A.6 | engine | GET /api/memory/archive; POST /api/memory/archive/{id}/restore | @0cc2b299 src/server.ts:2262; src/server.ts:2283; public/app.js:2334 | The engine keeps archived facts and restores one. Purge all: POST /api/memory/archive/purge {confirm:"purge", count} (the owner's alone; count is how many the dialog showed, GET's `total`; a count that no longer matches removes nothing). It removes the archived facts and their kept versions; checkpoints and backups the owner took keep their own copies. |
| `memexp15` | A.6 | engine | GET /api/memory/export?format=jsonl; GET /api/memory/export | @0cc2b299 src/server.ts:2199; src/server.ts:4276 | JSONL export (memory.jsonl) and the full archive (JSON of every record). The engine's archive is a JSON file, not a .zip; name the saved file from what the route returns. |
| `memmore15` | A.6 | window |  |  | Opens the Memory menu; window-only. |
| `selfdo15` | A.6 | engine | POST /api/policy/approve; POST /api/self-development/requests/{id}/approve\|decline | @0cc2b299 src/server.ts:1856; src/self-development.ts:183; src/self-development-requests.ts:217; src/self-development-contract.ts:26 | Branch's own change goes through questions the owner answers: preparing the change against a written contract, widening it, and the draft pull request (git.remote) are each asked and answered with POST /api/policy/approve (yes or no). A change asked for from a chat app (/improve) is answered with the self-development requests route, whose yes needs the owner's contract terms, not one click. The review now shows the bounded diff first (see selfrev15); Approve and Publish stay greyed for the security review. |
| `selfrev15` | A.6 | engine | GET /api/self-development/requests | @0cc2b299 src/self-development-requests.ts:211; src/self-development-requests.ts:214; src/self-development-contract.ts:39 | The review shows real engine data: the requested change and, for Branch's own proposal, the contract terms (allowed paths, tools, tests, rollback) as the scope tiles. The bounded diff: GET /api/self-development/requests/{id}/diff (owner only; refused to keys). Before the yes it says nothing has changed yet; once prepared, the worktree's changes since the contract's commit, capped in files, bytes and lines, with files outside the allowed paths named. Prepared requests are listed too ("edits approved, ready to publish"). |
| `tidy15` | A.6 | engine | GET /api/memory/tidy | @0cc2b299 src/server.ts:2255; src/memory-hygiene.ts:10; public/memory-tidy.js:45 | The rows are the engine's findings: said twice (duplicates), disagree (contradictions), long unused. Also GET /api/memory/tidy/all for the four checks together. The engine's stale age defaults to 180 days, not 120. |
| `tidydo15` | A.6 | engine | POST /api/memory/tidy; POST /api/memory/proposals/{id}/accept\|reject | @0cc2b299 src/server.ts:2256; src/memory-hygiene.ts:164; src/memory-hygiene.ts:170; src/server.ts:2271 | Merge them / Keep the newer one / Archive it are the engine's staged suggestions (merge, archive the older, forget), each applied by accepting its proposal; Leave it rejects it. Nothing is removed without that yes. The first pass said 'needs memory cleanup backend'; it exists. |
| `tt15` | A.6 | engine | GET /api/flows-boards/flows/{run}/steps | @0cc2b299 src/flows-boards/api.ts:60; src/flows-boards/time-travel.ts:9; public/flows-boards.js:110 | 'What it had at that moment' is the engine's kept state after every box of a graph-flow run. Only graph flows record steps, and only while that part is on. |
| `ttback15` | A.6 | engine | POST /api/flows-boards/flows/{run}/fork | @0cc2b299 src/flows-boards/api.ts:61; src/flows-boards/time-travel.ts:14; public/flows-boards.js:123 | Exactly the row: runs a copy from the chosen step and never touches the original. The first pass cited POST /api/sessions/{id}/rewind from docs/api.md, which is conversation rewind, not this. |
| `verify15` | A.6 | engine | POST /api/safety-extras/activity/verify | @0cc2b299 src/safety-extras/api.ts:123; public/safety-extras.js:193 | The engine checks the tamper-evident activity chain and answers the result (count and chain head for the Technical level). Not a window-only animation. |
| `addcomp` | A.7 | window |  |  | Opens 'Add a computer or phone' on its first tab; window-only. What each tab can do is judged under ac-tab / ac-pair. |
| `comp-add` | A.7 | window |  |  | Opens the 'Add a computer' kind picker; window-only. |
| `comp-add-go` | A.7 | engine | POST /api/devices/invite (pair); POST /api/remotes (SSH) | @0cc2b299 src/devices/api.ts:119; src/sandbox-remote-api.ts:93; src/remote/ssh-workspace.ts:14; public/devices.js:237 | Two of the four kinds are real: 'Another computer with Branch' makes a one-time invitation code (then the owner approves the request), and 'remote desktop or SSH' is the engine's SSH remote workspace (by the alias in ~/.ssh/config). Draw 'A new private computer on this PC' and 'A KeepOak cloud computer' as Coming soon: the Windows Sandbox backend only holds scripts, and there is no cloud provisioning. |
| `comp-chip` | A.7 | soon |  | @0cc2b299 src/trunks/record.ts:35; src/devices/tools.ts:68 | A Trunk has no list of allowed computers. The engine keeps one computer per Trunk (startsIn) and one picked device per conversation (devices/pick); a multi-computer allow-list is missing. The first pass's route (POST /api/trunks/{id}) cannot hold it. |
| `comp-cloud` | A.7 | soon |  | @0cc2b299 src/devices/book.ts:27 | No KeepOak cloud computer provisioning anywhere in src/ (no route, no service); computers are only paired devices, SSH remotes and the shared Linux desktop. |
| `comp-max` | A.7 | soon |  | @0cc2b299 src/trunks/record.ts:26 | No 'at once' limit on how many computers a Trunk uses: TrunkSchema (strict) has no such field and nothing in src/trunks reads one. |
| `comp-pick` | A.7 | window |  |  | Opens the computers popover; window-only. |
| `comp-set` | A.7 | engine | POST /api/devices/pick | @0cc2b299 src/devices/api.ts:125; src/devices/tools.ts:68; public/devices.js:318; src/trunks/starts-in.ts:56 | 'Uses the [computer] from its next step' is the engine's per-conversation device pick: it sends the device tools (screen, files, run...) of that conversation to the chosen paired device from the next step; for a Trunk pick on its chatSessionId. Not startsIn: POST /api/trunks/{id} {startsIn} saves, but every turn meant for another computer is then refused. Missing: 'won't use a computer' (deviceId null means 'any device', not none). |
| `comp-toggle` | A.7 | soon |  | @0cc2b299 src/devices/api.ts:79; src/trunks/record.ts:35 | Multi-select 'may use N computers' has no engine form: the device pick is one device per conversation and startsIn is one computer per Trunk. The first pass's POST /api/trunks/{id} cannot hold a list. |
| `dev-col` | A.7 | soon |  | @0cc2b299 src/devices/book.ts:27 | A paired device's record (strict schema) has name, platform, capabilities, folder and sharing, no colour; a chosen colour would be dropped. Changed from window: choosing it without saving it is faking. |
| `dev-glyph` | A.7 | soon |  | @0cc2b299 src/devices/book.ts:27 | No icon/glyph field on a paired device (strict DeviceRecordSchema); only the name can be saved (rename). Changed from window for the same reason as dev-col. |
| `dev-save` | A.7 | engine | POST /api/devices/requests/{id}; POST /api/devices/{id}/rename | @0cc2b299 src/devices/api.ts:131; src/devices/api.ts:74; src/devices/api.ts:88; public/devices.js:136 | Pairing is real: the owner lets the waiting device in (approve with codeMatches: true after comparing the check code) and the engine names it (rename). Only the name persists (see dev-col/dev-glyph). Reached after a code pairing, since network discovery (ac-pair) is not in the engine. |
| `devtab` | A.7 | window |  |  | Switches Customize tabs; window-only. |
| `edit` | A.7 | window |  |  | Opens the Trunk editor from the Trunk already listed (GET /api/trunks returns the roster; GET /api/trunks/{id} adds routines and receipts). Opening is window-only; saving is st-save / emo15 via POST /api/trunks/{id}. The first pass called the opener live with the save route. |
| `emo15` | A.7 | engine | POST /api/trunks/{id} | @0cc2b299 src/trunks/api.ts:124; public/settings-trunks.js:84; src/trunks/look.ts:15; src/trunks/look.ts:27 | Saved as look { face: 'emoji', emoji }; 'None' sets face back to pattern/letters. |
| `flow` | A.7 | engine | GET /api/flows/{id} | @0cc2b299 src/flows.ts:347; public/flow-editor.js:146 | Opening a saved automation loads its real steps from the engine (the old flow editor does exactly this). Changed from window: the steps are not in the page until fetched. |
| `flow-add` | A.7 | window |  |  | Adds a draft step in the editor; nothing is saved until flow-save. |
| `flow-mv` | A.7 | window |  |  | Reorders draft steps in the editor; window-only until flow-save. |
| `flow-rm` | A.7 | window |  |  | Removes a draft step in the editor; window-only until flow-save. |
| `flow-run` | A.7 | engine | POST /api/flows/{id}/run | @0cc2b299 src/flows.ts:350; public/flow-editor.js:243 | The engine runs a saved flow (and resume/pause). The first pass said 'needs backend' while citing only docs; the route is in src/flows.ts. |
| `flow-save` | A.7 | engine | POST /api/autonomy/procedures/{id}/propose, then POST /api/autonomy/decide | src/autonomy/procedures.ts proposeChange/applyChange; public/app/flows/flow-editor.js | The window's flow editor opens Automations › Procedures rows, which are procedures that start themselves (GET /api/autonomy/procedures, steps a Trunk is asked to do or that ask first) and saved recipes (GET /api/state procedures, tool calls). Only the first kind can change: Save shows the prototype's "Change …?" difference and Approve is the same ledger yes a new procedure gets, kept under the same id with the version before in its history. Recipes stay read-only (no route saves edited recipe steps). The earlier POST /api/flows citation was the old graph-flow editor, which this window does not open. |
| `grp-make` | A.7 | engine | POST /api/trunks/rooms | @0cc2b299 src/trunks/api.ts:106; src/trunks/rooms.ts:26; src/trunks/room-plan.ts:17 | The engine makes a group room with a name, 2 to 6 Trunks and up to 8 people. The first pass cited this very route but still said soon. Note: at least two Trunks (the prototype allows one), agents on other computers cannot be members, and the 'talk to each other' switch is not per room. Rooms switch must be on. |
| `grp-new` | A.7 | window |  |  | Opens the new group chat dialog; window-only. |
| `grp-pick` | A.7 | window |  |  | Picks members in the dialog; sent on grp-make. The 'Agents on other computers' chips have no engine form (RoomCreateSchema takes Trunk and person ids only) and should be drawn as Coming soon. |
| `grp-rule` | A.7 | soon |  | @0cc2b299 src/trunks/room-plan.ts:9; src/trunks/rooms.ts:26 | The engine has one fixed rule: @mentioned members answer, nobody mentioned means everyone. There is no per-room 'who answers' setting, so 'A lead Trunk decides' and 'Everyone, every time' cannot be saved. Show the mention rule as the fixed behaviour. |
| `mk-create` | A.7 | soon |  | @0cc2b299 src/trunks/index.ts:220 | Makes the Trunk Branch proposed. No proposal ever exists: no tool lets the assistant propose or create a Trunk (the only caller of trunks.create outside the owner's route is the update keeper in src/update-fix.ts). The Appendix row's 'leaf-burst animation' undersells it; the prototype makes the Trunk. |
| `mk-go` | A.7 | soon |  | @0cc2b299 src/trunks/index.ts:220 | 'Have Branch make a Trunk' needs Branch to answer with a structured proposal (name, face, tools, computer, schedule). No such tool or route; sending the words as a chat message would get prose, not a proposal. |
| `mk-new` | A.7 | soon |  | @0cc2b299 src/trunks/index.ts:220 | Opens a dialog whose only action (mk-go) the engine cannot do, so the + menu entry is drawn as Coming soon. The owner's own 'New Trunk' (POST /api/trunks) and templates (tmpl) stay live. |
| `mk-no` | A.7 | soon |  | @0cc2b299 src/trunks/index.ts:220 | Declines a Trunk proposal, which the engine never makes (see mk-go). |
| `pat15` | A.7 | soon |  | @0cc2b299 src/orchestration.ts:19; src/orchestration-modes.ts:11 | The engine has supervisor, swarm and router as tools the model picks per job; there is no owner setting for a default pattern 'used for rooms and big tasks', and no 'in parallel' or 'teams' default. OrchestrationSettingsSchema holds only autoPlan, planApproval, verify, milestones and stuck action. |
| `st-anim` | A.7 | engine | POST /api/trunks/{id} | @0cc2b299 src/trunks/api.ts:124; public/settings-trunks.js:84; src/trunks/look.ts:17 | Saved as look.motion. Engine motions are none, breathe, sway, shimmer, pulse, dots: 'bob' is not one; map it to sway or offer the engine's list. |
| `st-colour` | A.7 | engine | POST /api/trunks/{id} | @0cc2b299 src/trunks/api.ts:124; public/settings-trunks.js:84; src/trunks/record.ts:42 | A swatch hex is saved as chosenColour (lower-case #rrggbb); a series colour as look.colour. |
| `st-eyes` | A.7 | soon |  | @0cc2b299 src/trunks/look.ts:22; src/trunks/avatar.ts:29 | No eye choice in the engine: TrunkLookSchema (strict) has face, letters, emoji, shuffle, colour, shape, motion, depth; the drawn face's eyes come from a hash of the name (faceFor) and the drawn face was retired (DG-108). Round / wide / sleepy cannot be saved. First pass marked it live. |
| `st-save` | A.7 | engine | POST /api/trunks/{id} | @0cc2b299 src/trunks/api.ts:124; public/settings-trunks.js:84; public/studio.js:124 | Saves the whole editor draft (name, look, instructions, tools...) through the Trunk edit route. |
| `st-shape` | A.7 | engine | POST /api/trunks/{id} | @0cc2b299 src/trunks/api.ts:124; public/settings-trunks.js:84; src/trunks/look.ts:16 | Saved as look.shape. The design stores a shape index; the engine takes names (circle, squircle, leaf, acorn, shield, hexagon, pebble), so map the index to a name. |
| `st-shuffle` | A.7 | window |  |  | Randomises the editor draft; saved only by st-save. Its eye pick is part of st-eyes (soon). |
| `st-tab` | A.7 | window |  |  | Switches editor tabs; window-only. |
| `tmpl` | A.7 | engine | POST /api/trunks; POST /api/trunks/{id} | @0cc2b299 src/trunks/api.ts:93; src/trunks/api.ts:124; src/trunks/record.ts:20 | Create takes name, title, description; the template's colour and shape follow with an edit (look / chosenColour). The first message is POST /api/trunks/{id}/say or the Trunk's own conversation. |
| `ag-add` | A.8 | engine | POST /api/agents/remote (A2A card); POST /api/agents/pair (Branch on another computer) | @0cc2b299 src/a2a-routes.ts:103; src/a2a-routes.ts:105 | The engine adds an agent by its A2A card address and pairs with another Branch by link. The third kind, 'An agent on your KeepOak computer', has no engine form: draw it as Coming soon. First pass said 'needs backend persistence'. |
| `cli-add` | A.8 | soon |  | @0cc2b299 src/integrations/shell-config.ts:10; src/knobs/launch-file.ts:23; src/remote/ssh-workspace.ts:42 | Commands on this computer run only the executables named in the launch settings file (BRANCH_INTEGRATIONS shell.executables), which no route writes: the launch-file card changes only timeout, output size, offline and browser sites. Nothing finds CLIs on this computer either. (SSH remotes do carry their own program list, set when adding one.) |
| `mcp-add` | A.8 | soon |  | @0cc2b299 src/integrations/bootstrap.ts:299; src/mcp-workbench.ts:9; src/knobs/launch-file.ts:23 | Outside MCP servers come only from the launch settings file read at start (config.mcp); no route adds one, and there is no MCP catalogue. The engine can sign in to a server (POST /api/mcp/signin) and try it (mcp-test), but cannot keep it. Project .mcp.json files are only listed for folder trust, never written. First pass marked it live with a made-up 'POST /api/mcp/...'. |
| `mcp-cat` | A.8 | window |  |  | Filters the catalogue list in the dialog; window-only (the catalogue itself is soon, see mcp-add). |
| `mcp-save` | A.8 | soon |  | @0cc2b299 src/integrations/bootstrap.ts:299; src/mcp-workbench.ts:8 | Keeping your own server means writing it into the launch settings file; no route does that (see mcp-add). The same dialog's 'Test it' is live (mcp-test). |
| `mcp-test` | A.8 | engine | POST /api/mcp/try | @0cc2b299 src/server.ts:3133; src/mcp-workbench.ts:2; public/mcp-workbench.js:111 | The engine opens the server (a command or a web address) for the try, lists its tools with their forms, and closes it; the result is the real tool list, not a sample. |
| `pal` | A.8 | window |  |  | Runs a palette entry, which dispatches another action (judged on its own). Window-only. |
| `ph-lib` | A.8 | window |  |  | Phone navigation to Library; window-only. |
| `ph-lib8` | A.8 | window |  |  | Switches the phone Library tab; window-only. |
| `plug-add` | A.8 | engine | POST /api/plugin-catalog/add-ons/bundled/install; POST /api/plugin-catalog/add-ons/install | @0cc2b299 src/add-ons/api.ts:45; src/add-ons/api.ts:34; src/add-ons/package-shelf.ts:201; public/add-ons.js:58 | The engine installs add-on packages (bundled ones, or from a source after a look), adding their skills and plugin file. An outside server inside a pack is not connected (see mcp-add). Packages ship switched off. First pass said 'needs storage'. |
| `prompt-new` | A.8 | window |  |  | Opens the new saved prompt dialog; window-only. |
| `prompt-save` | A.8 | engine | POST /api/prompts | @0cc2b299 src/prompt-library-api.ts:74; src/prompt-library.ts:43 | The engine's prompt library saves a named prompt with its own /command and {blanks}; 'Try on two models' is POST /api/prompts/try. First pass said 'needs backend persistence'. |
| `replay` | A.8 | engine | GET /api/runs/{id}/recording; POST /api/runs/{id}/recording/flow | @0cc2b299 src/run-recording-api.ts:6; src/run-recording-api.ts:81; src/run-recording-api.ts:85; src/run-recording-api.ts:9; public/recordings.js:106 | Watching a task again is its recording (frames to step and play) and 'make a workflow' saves the recording as a flow. The first pass cited GET /api/runs/{id}/replay: that route is POST and re-runs the task ('Do this again'), a different thing. Recordings are behind their own switch (requireRecordings), off until the owner turns them on. |
| `rev` | A.8 | engine | POST /api/skill-revisions/try; POST /api/skill-revisions/accept; POST /api/skill-revisions/reject | @0cc2b299 src/orchestration-api.ts:138; src/orchestration-api.ts:139; src/orchestration-api.ts:146 | The revision card's Practice run / Keep it / Throw it away are the engine's skill revision try, accept and reject. Changed from window: each one changes or tests the skill. |
| `rp` | A.8 | window |  |  | Steps or plays the recording frames already loaded by replay; window-only. |
| `sk-draft` | A.8 | soon |  | @0cc2b299 src/skill-authoring.ts:180; src/reflection/api.ts:13 | Branch drafting a SKILL.md from typed words has no route. The drafter exists (draftNewSkill) but is reached only from a conversation's look-back (POST /api/reflection/learn needs a sessionId). The prototype writes a fixed template, so window would be faking. |
| `sk-save` | A.8 | soon |  | @0cc2b299 src/server.ts:2982; src/skill-authoring.ts:180 | 'Add skill' sits in the 'Write a skill with Branch' dialog and installs what Branch drafted from the typed words; drafting is sk-draft (soon), so there is nothing to install. The install route itself is real (POST /api/skills/install) and is reached through sk-src for a file, a registry or git, so greying this button loses nothing. First pass: live. |
| `sk-src` | A.8 | engine | POST /api/registry/install (library); POST /api/skills/install or /api/skills/package/install (file); POST /api/reach/git/install (GitHub) | @0cc2b299 src/server.ts:1746; src/server.ts:2987; src/reach/api.ts:102 | Three of the four sources are real: a registry the owner follows, a SKILL.md or package file, and a git repository (new skills arrive switched off). 'Write one with Branch' leads to sk-draft (soon). First pass said soon. |
| `sugg15` | A.8 | engine | GET /api/skills/suggest; POST /api/registry/install | @0cc2b299 src/server.ts:2986; src/skill-suggest.ts:5; public/skills-extra.js:120 | Suggestions are the engine's word match of recent tasks against switched-off skills and registry skills; adding one installs it switched off. First pass route 'POST /api/skills/...' was vague. |
| `t9-kind` | A.8 | window |  |  | Switches the Tools kind; window-only. |
| `t9-own` | A.8 | window |  |  | Opens the 'Add your own MCP server' dialog; window-only. In it, Test is live (mcp-test) and Save is soon (mcp-save). |
| `t9-sel` | A.8 | window |  |  | Selects a tool in the list; window-only. |
| `tool-add` | A.8 | window |  |  | Opens the add dialog for a kind; window-only. Each add inside is judged on its own: skills (sk-src) and plugins (plug-add) live, MCP (mcp-add) and CLIs (cli-add) soon. The first pass greyed the whole opener, which would hide the live skill and plugin adds. |
| `tool-rm` | A.8 | engine | POST /api/skills/{id}/remove; POST /api/plugin-catalog/add-ons/remove; POST /api/plugins/{id}/disable; POST /api/agents/remote/remove | @0cc2b299 src/server.ts:3016; src/add-ons/api.ts:44; src/server.ts:1785; src/a2a-routes.ts:104 | Skills, add-on packages, plugins and remote agents can be removed. An MCP server or a CLI cannot (they live in the launch settings file), so hide Remove for those kinds. Undo re-adds through the same add route. |
| `tool-who` | A.8 | engine | POST /api/trunks/{id} | @0cc2b299 src/trunks/api.ts:124; public/settings-trunks.js:84; src/trunks/record.ts:56; src/trunks/record.ts:54; public/settings-trunks.js:74 | Each Trunk's record says which connected tool servers it may use (mcpServers, none by default), which skills it reaches for first (skills) and which tool permissions it has; the chip toggles the Trunk in or out of that list. Plugins, CLIs and remote agents have no per-Trunk list. |
| `tools-manage` | A.8 | window |  |  | Navigates to Customize > Tools; window-only. |
| `tools9` | A.8 | window |  |  | Opens the composer's tools popover; window-only. |
| `widget6` | A.8 | engine | GET /api/embeds; GET /api/listen | @0cc2b299 src/orchestration-api.ts:77; src/server.ts:1212; src/embeds.ts:26; src/server.ts:699; public/widget.js:16 | The widget is real: served at /widget.js, it talks only to the paired remote listener, and its switch and allowed sites are GET/POST /api/embeds (off by default). The snippet is <script src=.../widget.js data-branch=<listener address> data-key=<pairing key>>; the listener's address comes from GET /api/listen (what the old Deployment card reads), but the engine never hands the pairing key back, so the snippet shows a placeholder for data-key that the owner fills in, as the prototype masks it (bw_...). |
| `ch-fam` | A.9 | window |  |  | Filters the chat app grid; window-only. |
| `ch-open` | A.9 | engine | GET /api/channel-setup/{id} | @0cc2b299 src/channel-setup/api.ts:47; public/channel-setup.js:268 | The wizard's steps come from the engine's setup panel for that app: the command, the links, the square codes and the fields to paste. Changed from window: the content is engine data. |
| `chf-eye` | A.9 | window |  |  | Shows or hides a secret field; window-only. |
| `chw-back` | A.9 | window |  |  | Wizard back; window-only. |
| `chw-next` | A.9 | engine | POST /api/channel-setup/{id}/check (Check step); POST /api/channels/pairings/approve (Pair step) | @0cc2b299 src/channel-setup/api.ts:39; src/server.ts:2801; public/app.js:846 | Continuing into Check sends the pasted values to the engine's read-only check with the service (which also saves them), and 'Approve' on the Pair step approves the six-digit pairing code. Only moving between steps with nothing to send is window-side. |
| `chw-save` | A.9 | engine | POST /api/channel-setup/{id}/check | @0cc2b299 src/channel-setup/api.ts:44; src/channel-setup/service.ts:80 | Saving with enable: 'on' checks the values, keeps them in the locker and switches the app on: Telegram connects, the 36 'switch' apps flip their channel switch. For the 18 'file' apps (Discord, Slack, WhatsApp, email, Signal, Matrix...) the values are checked and kept, but the app only starts once its entry (shown by the panel) is in the connections file, so the Save step must say that rather than 'is connected'. 'Who may message it' maps to POST /api/channels/permissions. |
| `ph-ch13` | A.9 | window |  |  | Phone navigation to a chat app; window-only. |
| `ph-ch13-off` | A.9 | engine | POST /api/channels/parity | @0cc2b299 src/server.ts:2784; src/channels/parity-api.ts:10 | Disconnect is the engine's per-app switch: POST { <app>: 'off' } closes a connected channel at once. The first pass cited the check route and called it soon. Covers the 36 'switch' apps (IRC, Twitch, Teams bot, Mastodon, Bluesky, SMS, ntfy...) and Telegram (its setup route with enable). The 18 'file' apps (Discord, Slack, WhatsApp, email, Signal, Matrix, Messenger, Instagram, Mattermost, Rocket.Chat, Google Chat, MS Teams, Zulip, Feishu, DingTalk, WeCom, LINE, Viber) are switched on only by an entry in the connections file, which no route writes: draw their Connect/Disconnect as Coming soon (data/channel-setup.json turnOn). |
| `ph-ch13-on` | A.9 | engine | POST /api/channels/parity | @0cc2b299 src/server.ts:2784; src/channels/parity-api.ts:10 | Connect is the same switch set to 'on' for an app that is already set up; one that is not set up needs the setup wizard (ch-open). Covers the 36 'switch' apps (IRC, Twitch, Teams bot, Mastodon, Bluesky, SMS, ntfy...) and Telegram (its setup route with enable). The 18 'file' apps (Discord, Slack, WhatsApp, email, Signal, Matrix, Messenger, Instagram, Mattermost, Rocket.Chat, Google Chat, MS Teams, Zulip, Feishu, DingTalk, WeCom, LINE, Viber) are switched on only by an entry in the connections file, which no route writes: draw their Connect/Disconnect as Coming soon (data/channel-setup.json turnOn). |
| `ph-chf13` | A.9 | window |  |  | Phone chat apps filter; window-only. |
| `aa-back` | A.10 | window |  |  | Add-account wizard back; window-only. |
| `aa-done` | A.10 | engine | POST /api/accounts/add; POST /api/connections/from-preset | @0cc2b299 src/accounts/api.ts:29; src/accounts/manage.ts:21; src/server.ts:2315 | Adds another account (label, key into the locker) to a connection; a service with no connection yet is added from the catalogue first (from-preset, checked before saving). Several accounts per connection ships switched off (409 until on). |
| `aa-gone` | A.10 | window |  |  | Toast explaining a retired service; window-only. |
| `aa-grp` | A.10 | window |  |  | Switches the service group in the wizard; window-only. |
| `aa-key` | A.10 | window |  |  | Advances to the key step; window-only (the key is sent on aa-done). |
| `aa-local` | A.10 | window |  |  | Navigates to Settings > On this computer; window-only. |
| `aa-nm` | A.10 | window |  |  | Fills the account name field; window-only. |
| `aa-pos` | A.10 | window |  |  | Chooses where the new account goes in the draft; applied after aa-done with POST /api/accounts/update { move }. |
| `aa-prov` | A.10 | engine | POST /api/chatgpt/login; POST /api/models/gemini-signin; POST /api/connections/oauth/start; POST /api/providers/cli-agents | @0cc2b299 src/server.ts:2850; src/server.ts:1519; src/server.ts:2302; src/server.ts:1468; src/accounts/terms.ts:15 | Picking a key-based service is window-side, but picking a plan service starts the engine's sign-in, so this is wired, not window-only. Plan (sign-in) services map as follows: ChatGPT -> ChatGPT device sign-in (POST /api/chatgpt/login; an extra ChatGPT account: POST /api/accounts/chatgpt/login); Gemini -> POST /api/models/gemini-signin; services that use OAuth (Google, Microsoft, GitHub, Slack...) -> POST /api/connections/oauth/start; Claude Code, Codex, Gemini CLI and Copilot CLI -> POST /api/providers/cli-agents (Branch runs the installed program, which keeps its own sign-in). Missing: an in-app Claude (claude.ai) sign-in and a GitHub Copilot plan sign-in; the engine never handles Claude.ai sign-ins by design (src/accounts/terms.ts), so draw those two cards as Coming soon and point to Claude Code / Copilot CLI. |
| `aa-tr` | A.10 | window |  |  | Chooses which Trunks use the new account in the draft; applied after aa-done through each Trunk's keys.accounts (POST /api/trunks/{id}). |
| `ac-pair` | A.10 | soon |  | @0cc2b299 src/a2a-routes.ts:96; src/devices/api.ts:119 | Pairing a computer 'found on your network' needs discovery, and the engine has none: no mDNS or network scan anywhere in src/, and GET /api/agents/discover only probes addresses the owner types. Pairing works with a code (devices invite, see comp-add-go). Changed from window. |
| `ac-tab` | A.10 | window |  |  | Switches tabs in 'Add a computer or phone'; window-only. |
| `acct-first` | A.10 | engine | POST /api/accounts/pool | @0cc2b299 src/accounts/api.ts:31; src/accounts/manage.ts:40; src/accounts/pool.ts:126 | 'Answers first' is the pool's defaultAccount (or pinned via POST /api/accounts/update). First pass route was 'POST /api/accounts/...'. |
| `acct-menu` | A.10 | window |  |  | Opens the account menu; window-only. |
| `acct-out` | A.10 | engine | POST /api/accounts/remove; POST /api/accounts/chatgpt/logout | @0cc2b299 src/accounts/api.ts:32; src/accounts/api.ts:36 | Removes the account from the list; a ChatGPT sign-in account signs out first. The first account of a connection is the connection itself (removed with POST /api/connections/forget). |
| `acct-up` | A.10 | engine | POST /api/accounts/update | @0cc2b299 src/accounts/api.ts:30; src/accounts/manage.ts:36 | { pool, account, move: 'up' }. |
| `addacct` | A.10 | window |  |  | Opens the Add an account dialog; window-only (its service list can come from GET /api/connections/catalog). |
| `low-x` | A.10 | window |  |  | Hides the 'nearly used up' chip for this window; window-only (the prototype sets S.lowDismissed). |
| `owner` | A.10 | window |  |  | Opens the owner menu; window-only. |
| `ph-mode` | A.10 | engine | POST /api/conversation-mode | @0cc2b299 src/conversation-mode-api.ts:18; src/conversation-mode-api.ts:31; src/conversation-mode-api.ts:89 | Sets this conversation's mode { sessionId, mode }; the engine enforces it and refuses one looser than the owner's setting or Lockdown. Changed from window: a mode that only lives in the window would not change what the task may do. |
| `ph-model` | A.10 | engine | POST /api/sessions/{id}/model | @0cc2b299 src/server.ts:2118; public/app.js:1415 | Picks the conversation's model preset { preset }. Changed from window: otherwise the reply would not use it. |
| `ph-plus` | A.10 | window |  |  | Closes the phone 'Add to message' sheet and toasts about the option; window-only as described. |
| `pick-model` | A.10 | engine | POST /api/sessions/{id}/model | @0cc2b299 src/server.ts:2118; public/app.js:1415 | Sets the conversation's model preset (and, for the Qwen case, the mode via POST /api/conversation-mode). Changed from window. |
| `pick-think` | A.10 | engine | POST /api/sessions/{id}/model | @0cc2b299 src/server.ts:2118; public/app.js:1417 | Thinking level is the session's reasoning effort in the same route { reasoning }; the levels offered come from the model's own list. Changed from window. |
| `signin` | A.10 | engine | POST /api/connections/oauth/start; POST /api/chatgpt/login; POST /api/models/gemini-signin; POST /api/providers/cli-agents | @0cc2b299 src/server.ts:2302; src/oauth.ts:9; src/server.ts:2850; src/server.ts:1519; src/server.ts:1468; src/accounts/terms.ts:15 | 'Continue on their site' is the engine's sign-in: OAuth with PKCE in the owner's browser (key straight into the locker), ChatGPT's device sign-in, or Gemini's. First pass said 'needs OAuth backend'; it exists. Plan (sign-in) services map as follows: ChatGPT -> ChatGPT device sign-in (POST /api/chatgpt/login; an extra ChatGPT account: POST /api/accounts/chatgpt/login); Gemini -> POST /api/models/gemini-signin; services that use OAuth (Google, Microsoft, GitHub, Slack...) -> POST /api/connections/oauth/start; Claude Code, Codex, Gemini CLI and Copilot CLI -> POST /api/providers/cli-agents (Branch runs the installed program, which keeps its own sign-in). Missing: an in-app Claude (claude.ai) sign-in and a GitHub Copilot plan sign-in; the engine never handles Claude.ai sign-ins by design (src/accounts/terms.ts), so draw those two cards as Coming soon and point to Claude Code / Copilot CLI. |
| `dl-go` | A.11 | engine | POST /api/local-models/pull; GET /api/local-models/downloads | @0cc2b299 src/local-models-api.ts:80; src/local-models-api.ts:52; src/local-runtimes.ts:63 | Real download with real progress (poll downloads). The prototype's timer is not used. The older Ollama routes work whatever the local-models switch says. |
| `download` | A.11 | window |  |  | Opens the 'Get another model' dialog; window-only (what fits this computer can come from POST /api/local-models/offers). |
| `lm-chat` | A.11 | window |  |  | Opens the conversation; window-only. Chatting with the local model is its preset picked through pick-model. |
| `lm-get` | A.11 | engine | POST /api/local-models/pull | @0cc2b299 src/local-models-api.ts:80; src/local-runtimes.ts:63 | Starts the download of the chosen model tag. |
| `lm-rm` | A.11 | engine | POST /api/local-models/remove; POST /api/local-models/delete | @0cc2b299 src/local-models-api.ts:82; src/local-models-api.ts:101 | remove for Ollama models; delete for the one-click manager's models. |
| `lm-run` | A.11 | engine | POST /api/local-models/runtime/start; POST /api/local-models/load | @0cc2b299 src/local-models-api.ts:103; src/local-models-api.ts:83 | Starts the local runtime (Ollama answers on 11434) or loads the model in LM Studio. The first pass cited POST /api/local-models/switch, which is the on/off switch for the whole feature, not running a model. |
| `lm-stop` | A.11 | engine | POST /api/local-models/stop | @0cc2b299 src/local-models-api.ts:81; src/local-runtimes.ts:103 | Stops a download that is still going. |
| `lm-v` | A.11 | window |  |  | Chooses which variant (tag) the next download fetches; nothing is sent until lm-get / dl-go pulls that tag. The first pass called the selection itself live. |
| `acbulk15` | A.12 | engine | POST /api/accounts/update; POST /api/accounts/remove | src/accounts/api.ts:30; src/accounts/api.ts:32; src/accounts/manage.ts:36; src/accounts/manage.ts:116; public/accounts.js:155; public/accounts.js:167; public/accounts.js:169 | Pause = update {disabled:true}; Sign out = /remove; Move to the top = repeated update {move:'up'} (engine only moves one place at a time). NOT settings-kit: accounts are on its neverTouched list (src/settings-kit/catalogue.ts). |
| `acsel15` | A.12 | window |  |  | Turns tick-boxes and the bulk bar on or off; window state only. |
| `ask16` | A.12 | window |  |  | Triggers the real OS permission prompt from the window (getUserMedia / Notification.requestPermission, as public/voice.js and public/app.js do); no engine route. The drawn macOS alert itself is askdo16. |
| `askdo16` | A.12 | proto |  |  | Don't Allow / Allow belong to the operating system's own alert; Branch never draws them. The prototype fakes the macOS dialog. |
| `doctor` | A.12 | engine | GET /api/deployment/doctor?fix=1 | src/deployment-api.ts:194; public/deployment.js:184 | The final prototype handler (Check and fix Branch dialog listing engine, settings file, database, disk...) matches the engine's doctor check-and-fix; the first pass read the older 2.1 s demo handler. |
| `editfile` | A.12 | engine | GET /api/settings-kit/files/{slot} | src/settings-kit/api.ts:210; src/settings-kit/file-map.ts:82; public/agent-files.js:113 | Opens one of the owner's instruction files (SOUL, IDENTITY, USER, AGENTS, TOOLS, SOP, MEMORY, HEARTBEAT) with its text. |
| `eval-run` | A.12 | engine | POST /api/evaluation/run | src/server.ts:1808; src/server.ts:1798; public/evaluation.js:51 | Usage › 'Test the model you use' is the old window's evaluation card: runs a ready-made suite against the current model, returns right/wrong, cost and regressions. Suites from GET /api/evaluation/suites. |
| `hb-every` | A.12 | engine | POST /api/heartbeat | src/scheduler.ts:596; src/heartbeat.ts:56; public/heartbeat.js:137 | Saved key: settings 'heartbeat' field everyMinutes (5-1440). Not settings-kit. |
| `hb-rm` | A.12 | engine | POST /api/heartbeat | src/scheduler.ts:596; src/heartbeat.ts:60; public/heartbeat.js:138 | A check-in line is one line of the heartbeat checklist text; removing it saves the checklist without that line. |
| `if-back` | A.12 | engine | POST /api/settings-kit/files/undo | src/settings-kit/api.ts:230; src/settings-kit/file-map.ts:160; public/agent-files.js:148 | Engine keeps only the one version before the last save made here (lastSave/undoFile), and writes it back directly; it has no list of every earlier version and does not load one into the editor unsaved. |
| `if-open` | A.12 | engine | GET /api/settings-kit/files/{slot} | src/settings-kit/api.ts:210; public/agent-files.js:113 | Same engine read as editfile. |
| `if-owner` | A.12 | soon |  | src/context-files.ts:69; src/trunks/record.ts:46 | Choosing a Trunk shows that Trunk's own copy of each file; the engine keeps one shared set of files (slots scoped owner/work, no per-Trunk copies). A Trunk has only its own instructions text in its record (edited in Edit Trunk). 'Every Trunk' is the only real choice. |
| `if-save` | A.12 | engine | POST /api/settings-kit/files | src/settings-kit/api.ts:225; src/settings-kit/file-map.ts:112; public/agent-files.js:143 | Saves the file {slot,text} and keeps the text before for one undo. Not settings-kit/apply. |
| `if-write` | A.12 | engine | POST /api/tools/try {name:'project.init', arguments:{dryRun:true}} | src/server.ts:1363; src/coding/init.ts:26; src/coding/init.ts:93; src/coding/index.ts:69; src/coding/commands.ts:10 | AGENTS.md ONLY. project.init with dryRun returns a plain first draft from the project's own files (nothing written), which matches 'read it before you save'; the /init slash command (initCommand) instead sends the prompt as a chat turn so the model writes it. The tool exists only while the coding 'init' switch is on. No engine writer exists for SOUL/IDENTITY/USER/TOOLS/SOP/MEMORY/HEARTBEAT, so the button must be live on AGENTS.md only. The old file editor had no such button. |
| `key15` | A.12 | engine | POST /api/comfort | src/comfort/api.ts:172; src/comfort/settings.ts:37; src/comfort/settings.ts:21; src/comfort/settings.ts:16; public/comfort.js:85 | Saved as settings card 'keys' {card:'keys', values:{...}}; engine refuses clashes and bare keys. Only 5 of the prototype's 10 rows have an engine key: Find anything=palette, New conversation=newConversation, Settings=appearance, side panel=sidePane, Stop the current task=stopTask. Focus mode, Talk live, Lockdown, Open the Inbox and Next conversation have no engine field (and 'Ctrl Tab' fails keyCombo), so those rows are soon. |
| `keyreset15` | A.12 | engine | POST /api/comfort | src/comfort/api.ts:172; src/comfort/settings.ts:21 | Save that one action back to its shortcutDefaults value (reset:true would put the whole card back). |
| `mtab` | A.12 | window |  |  | Switches tabs inside the Models dialog. |
| `pin-add8` | A.12 | window |  |  | Opens the popover listing settings not yet pinned (list comes from GET /api/settings-kit). |
| `pin-do8` | A.12 | engine | POST /api/settings-kit/pins | src/settings-kit/api.ts:222; src/settings-kit/api.ts:101; public/pins.js:62 | Body {key,field,pinned:true}: a pinned setting cannot be changed by anybody else on this computer, which is what the prototype's Pins page says. Only catalogue settings can be pinned (e.g. retention = 'Keep conversations'); Accounts is neverTouched so it cannot be offered. |
| `pin-ok` | A.12 | engine | POST /api/profiles/switch | src/collab-server.ts:211; src/collab-server.ts:223; public/collab.js:256 | Switching back to the owner with the owner's PIN is an engine route {profileId:null, pin}; the engine checks the PIN (off until the owner sets one via /api/profiles/owner-pin). |
| `pin-rm8` | A.12 | engine | POST /api/settings-kit/pins | src/settings-kit/api.ts:222; public/pins.js:62 | Body {key,field,pinned:false}. |
| `ptab` | A.12 | window |  |  | Switches a place's tab. |
| `rep15` | A.12 | engine | POST /api/usage/report | src/usage-report-api.ts:39; src/usage-report.ts:18; public/usage-report.js:74 | The card's cost and task count for 7/30/90 days come from the engine report with range '7d'\|'30d'\|'90d'. |
| `repopen15` | A.12 | engine | POST /api/usage/report | src/server.ts:2041; src/usage-report.ts:32; public/usage-report.js:74 | POST, not GET: {range, format}; a spreadsheet is another format. Needs the usage-report switch on (settings 'usage-report'). Engine report has no 'by person' split. |
| `restart16` | A.12 | engine | IPC branch:restart (desktop); POST /api/dashboard/restart | src/desktop/restart-ipc.ts:4; src/desktop/preload.cts:16; src/dashboard-api.ts:193; public/layout.js:1661 | Real relaunch through the preload bridge. The prototype's 'turns Screen Recording on' is the OS's doing, not Branch's. |
| `setgo` | A.12 | window |  |  | Navigation to a Settings page. |
| `setlevel` | A.12 | engine | POST /api/preferences | src/server.ts:1500; src/preferences.ts:23; public/settings-grown.js:67 | Saved key: settings 'preferences' field settingsLevel (regular\|advanced\|technical). The route replaces the whole record, so send the current preferences with it. |
| `setpage` | A.12 | window |  |  | Selects a Settings page. |
| `shortcuts` | A.12 | window |  |  | Opens the shortcuts sheet (the list itself comes from GET /api/comfort). |
| `sys16` | A.12 | engine | IPC branch:open-external (desktop, macOS) | src/desktop/updater-ipc.ts:150; src/desktop/updater-ipc.ts:31; src/os-permissions.ts:62; public/os-permissions.js:127 | Opens the real System Settings pane on a Mac. Only microphone, camera, screen and accessibility have allowed links; automation (auto), Full Disk Access (fda) and notifications (notif) are refused by the IPC allow-list today. Statuses before/after come from GET /api/os-permissions (macOS consent always reads 'unknown'). |
| `sysclose16` | A.12 | proto |  | src/server.ts:1542 | Closes the prototype's drawn copy of macOS System Settings. In the real app that window is the OS's; the re-check on return is GET /api/os-permissions when the window regains focus, not this control. |
| `sysflip16` | A.12 | proto |  |  | The switch beside Branch lives in macOS System Settings; Branch cannot draw or flip it. |
| `sysquit16` | A.12 | proto |  |  | The Later / Quit & Reopen sheet is macOS's own; the real relaunch is restart16. |
| `syssheet16` | A.12 | proto |  |  | Touch ID sheet belongs to macOS. |
| `systouch16` | A.12 | proto |  |  | Touch ID / password grant is done by macOS, never by Branch. |
| `updgo15` | A.12 | engine | IPC branch:update-install (desktop); POST /api/comfort/update-plan | src/desktop/updater-ipc.ts:104; src/desktop/updater-ipc.ts:65; src/comfort/api.ts:157; public/app.js:1194; public/app.js:1298 | 'wait' works (window polls busyTasks via update-plan, installs at 0). 'now' while tasks run is refused by the engine (ensureIdle defers while busyTasks > 0); an interrupted run can be continued afterwards (runs report canContinue), but the engine never cuts work off to install. |
| `updmenu-go` | A.12 | window |  |  | Closes the menu and opens Settings › Updates; navigation only (first pass wrongly cited settings-kit/apply). |
| `updpick15` | A.12 | window |  |  | Picks 'wait' or 'now' in the dialog; the engine is reached by updgo15. |
| `widthset` | A.12 | engine | POST /api/preferences | src/server.ts:1500; src/preferences.ts:27; public/layout.js:288 | Saved key: settings 'preferences' field conversationWidth (comfortable\|wide\|full). |
| `ws` | A.12 | window |  |  | Personal/Team switch changes the view; the Team workspace is keepoak.com's (sample until connected). First pass wrongly cited settings-kit/apply. |
| `about` | A.13 | engine | GET /api/state | src/server.ts:1348; src/server.ts:1028; public/layout.js:341 | About Branch is a real dialog; the version comes from the engine's state. Only the prototype's wording ('a design prototype with example data') is scaffolding. |
| `acc-save` | A.13 | window |  |  | Starts a custom theme from the current accent. The engine has no custom-theme store (/api/look accepts only catalogue theme ids), so custom themes live in the window. First pass wrongly cited settings-kit/apply. |
| `acc-set` | A.13 | window |  |  | The prototype offers 8 hex accents, the theme's own and any colour; the engine's preferences.accent is a 5-name enum (copper\|leaf\|earth\|slate\|ink) that cannot hold these, so the accent override is window-side. First pass wrongly cited settings-kit/apply. |
| `ag-hide` | A.13 | window |  |  | Hides the floating agent window; no engine setting exists for it (first pass wrongly cited settings-kit/apply). |
| `ag-min` | A.13 | window |  |  | Minimises the agent window. |
| `ag-size` | A.13 | window |  |  | Agent window size; no engine setting exists (first pass wrongly cited settings-kit/apply). |
| `bg-peek` | A.13 | window |  |  | Adds a class to clear the view. |
| `bg-remove` | A.13 | window |  |  | Opens the confirm dialog. |
| `bg-remove-yes` | A.13 | window |  |  | The background file is kept in the window's own IndexedDB and never sent to the engine; removing it deletes that storage (forgetBackground). First pass wrongly cited settings-kit/apply. |
| `bgfit` | A.13 | engine | POST /api/delight/settings | src/delight.ts:251; src/delight.ts:52; public/delight.js:209 | Saved key: settings 'delight' field background.fit (fill\|fit\|tile). |
| `bgset` | A.13 | engine | POST /api/delight/settings | src/delight.ts:251; src/delight.ts:48; public/delight.js:143 | Engine stores only background.on; which picture (own file, the built-in oak/acorn 3D, grove, rings) is window-side (IndexedDB / drawn by the window). |
| `ce-acc` | A.13 | window |  |  | Colour editor field for a custom theme; custom themes are window-side (no engine store). |
| `ce-cancel` | A.13 | window |  |  | Discards the edit. |
| `ce-fill` | A.13 | window |  |  | Derives colours in the window; no engine involvement (first pass wrongly cited settings-kit/apply). |
| `ce-mode` | A.13 | window |  |  | Switches light/dark in the editor. |
| `ce-new` | A.13 | window |  |  | Starts a custom theme; window-side (engine /api/look only accepts catalogue ids). |
| `ce-save` | A.13 | window |  |  | Saves a custom theme in the window; the engine cannot store a custom theme. |
| `hide` | A.13 | engine | POST /api/preferences | src/server.ts:1500; src/preferences.ts:29; public/panels-hide.js:217 | Saved key: settings 'preferences' field hidden (list of part ids). |
| `look-set` | A.13 | window |  |  | A Trunk's character look. The engine's Trunk look (src/trunks/look.ts TrunkLookSchema: face/shape/motion/colour, strict) has no character id, so the choice can only be kept by the window; 'classic' is the engine's existing look. |
| `my-code` | A.13 | window |  |  | Shows/copies a custom theme's code; window-side. |
| `my-del` | A.13 | window |  |  | Opens the confirm dialog. |
| `my-del-yes` | A.13 | window |  |  | Deletes a custom theme kept by the window. |
| `my-dup` | A.13 | window |  |  | Duplicates a custom theme kept by the window. |
| `my-edit` | A.13 | window |  |  | Opens the colour editor for a custom theme. |
| `my-paste` | A.13 | window |  |  | Opens the paste dialog. |
| `my-paste-go` | A.13 | window |  |  | Parses a theme code into a window-side custom theme. |
| `petset` | A.13 | engine | POST /api/delight/settings | src/delight.ts:251; src/delight.ts:32; src/achievements.ts:32; public/delight.js:59 | Saved key: settings 'delight' pets.on + pets.kind. squirrel/owl/hedgehog exist in the engine; 'sprout' (Little Branch) is not in petKinds. |
| `petwhere15` | A.13 | window |  |  | Where the pet walks (list, status bar, by the message box) is layout; the engine has no field for it. |
| `scene-set` | A.13 | window |  |  | Painted scenes are window art (public/art); the engine stores no scene. First pass wrongly cited settings-kit/apply. |
| `season` | A.13 | window |  |  | The old window keeps the season in localStorage 'branch-season'; the engine only notes it for achievements (POST /api/delight/noticed). First pass wrongly cited settings-kit/apply. |
| `size` | A.13 | engine | POST /api/preferences | src/server.ts:1500; src/preferences.ts:12; public/appearance.js:57 | Saved key: settings 'preferences' field textSize (small\|medium\|large). |
| `skin` | A.13 | engine | POST /api/look | src/server.ts:1499; src/terminal-theme.ts:236; src/terminal-theme.ts:214; public/look-sync.js:14 | Saved key: settings 'look' field theme; every prototype 't-<id>' skin is a catalogue theme id the engine validates and the terminal shares. 'branch' (Branch Slate) = DEFAULT_THEME 'slate'. 'paper' is not a catalogue theme and saveLook refuses it, so Paper is soon (or window-only). |
| `skin-ph` | A.13 | engine | POST /api/look | src/server.ts:1499; public/look-sync.js:14 | Same saved theme as skin, from the phone appearance page. |
| `skinf` | A.13 | window |  |  | Filters the skins gallery. |
| `skinprev` | A.13 | window |  |  | Previews light/dark in the gallery. |
| `skins` | A.13 | window |  |  | Opens the skins gallery. |
| `themeset` | A.13 | engine | POST /api/preferences | src/server.ts:1500; src/preferences.ts:7; src/preferences.ts:8; public/rail-foot.js:69 | Saved keys: preferences.appearance (forest=dark, daylight=light) and followSystem (system). |
| `firstrun` | A.14 | window |  |  | Replays the first-run screens. |
| `fr-acc` | A.14 | engine | POST /api/accounts/chatgpt/login (and the other sign-ins) | src/accounts/api.ts:35; public/accounts.js:189 | Signing in to an account on its own site is the engine's account sign-in; the row shows the engine's signed-in state. |
| `fr-next` | A.14 | window |  |  | Next first-run screen. |
| `fr-recs` | A.14 | engine | POST /api/never-break; POST /api/comfort | src/never-break/api.ts:61; src/comfort/settings.ts:72; public/never-break.js:81 | Gateway on = never-break mode 'on' (takes effect next start); update by itself = comfort card 'notify' autoUpdate 'install'. |
| `fr-skip` | A.14 | window |  |  | Leaves first run. |
| `fr-tmpl` | A.14 | engine | POST /api/trunks | src/trunks/api.ts:93 | Picking a template ends in creating that Trunk through the engine's create route; the six job templates (design doc 'START FROM A JOB') are design text, not engine data; the engine has no template list, so the Trunk is created from that text. |
| `fr-tour` | A.14 | window |  |  | Starts the walkthrough. |
| `fr-way` | A.14 | engine | POST /api/local-models/one-button; POST /api/accounts/chatgpt/login; POST /api/providers/cli-agents; POST /api/onboarding | src/local-models-api.ts:98; src/accounts/api.ts:35; src/server.ts:1468; src/server.ts:1492; public/local-oneclick.js:226; public/app.js:973 | computer = one-button local model download; ChatGPT = sign-in; Claude = the Claude Code program (cli-agents); practice = the old window's demo door (onboarding done). |
| `help` | A.14 | window |  |  | Starts the walkthrough. |
| `ob-close` | A.14 | window |  |  | Skip for now closes setup without marking it done. |
| `ob-done` | A.14 | engine | POST /api/onboarding | src/server.ts:1492; src/server.ts:785; public/app.js:991 | Saved key: settings 'onboarding' {done:true}; the step's gateway choice is saved with POST /api/never-break. First pass wrongly cited settings-kit/apply. |
| `ob-go` | A.14 | window |  |  | Jumps to a setup step. |
| `ob-gw` | A.14 | engine | POST /api/never-break | src/never-break/api.ts:61; public/never-break.js:81 | Gateway mode off\|when-needed\|on; saved to the gateway config (applied when setup finishes). NOT settings-kit: gateway is neverTouched there. |
| `ob-next` | A.14 | window |  |  | Next setup step. |
| `ob-propose` | A.14 | soon |  | src/trunks/api.ts:93 | The engine has no route that reads a description of the owner's life and proposes Trunks; it can only create a Trunk it is given. |
| `ob-set` | A.14 | window |  |  | Stores the 'where' / 'people' answer in the setup flow, which decides the next screen; no engine record (first pass wrongly cited settings-kit/apply). |
| `ob-test` | A.14 | engine | POST /api/models/test | src/server.ts:1461; public/app.js:981 | 'Say hello to test it' is the engine's model test (preset name, time, reply). |
| `ob-tpl` | A.14 | engine | POST /api/trunks | src/trunks/api.ts:93 | The ticked templates become Trunks through the engine's create route when setup finishes; the job templates are design text (design doc 'START FROM A JOB'), not engine data. |
| `ob15` | A.14 | engine | POST /api/preferences; POST /api/conversation-mode/settings; /api/personal/signin/*; POST /api/restore | src/server.ts:1500; src/conversation-mode-api.ts:90; src/personal/api.ts:105; src/server.ts:1720 | look -> preferences appearance/followSystem; asks -> the default mode for new conversations (conversation-mode settings newConversation); mail15 -> email/calendar sign-in; restore15 -> restore a backup. First pass wrongly cited settings-kit/apply. |
| `onboard` | A.14 | window |  |  | Opens setup at step 0. |
| `teach-start` | A.14 | engine | POST /api/settings-kit/apply | src/settings-kit/api.ts:221; src/settings-kit/catalogue.ts:431; public/first-run-next.js:61 | 'Show it how, once' = the old window's 'Watch me once': {plan:{source:'set',key:'run-recording',field:'mode',value:'when-needed'}} switches on recording the next task. This one IS a catalogue setting. |
| `teach-stop` | A.14 | engine | POST /api/runs/{id}/recording/flow | src/run-recording-api.ts:91; public/recordings.js:192 | Saves the recorded task as a workflow. The engine makes a flow (Automations), not an entry in autonomy procedures; POST /api/autonomy/procedures exists if it must be a procedure. |
| `tour` | A.14 | window |  |  | Starts the walkthrough. |
| `tour-back` | A.14 | window |  |  | Walkthrough navigation. |
| `tour-end` | A.14 | window |  |  | Ends the walkthrough. |
| `tour-next` | A.14 | window |  |  | Walkthrough navigation. |
| `welcome-x` | A.14 | window |  |  | Closes the welcome card. |
| `comp-view-ph` | A.15 | offscope |  |  | Phone app (separate product) |
| `lend15` | A.15 | offscope |  |  | Phone app (separate product) |
| `pair` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-back` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-back8` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-cf` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-in` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-island` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-new8` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-open` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-open8` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-paired` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-paired-dlg` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-scan` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-scr` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-shared` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-sheet` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-sheet-model` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-sw` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-tab8` | A.15 | offscope |  |  | Phone app (separate product) |
| `ph-try8` | A.15 | offscope |  |  | Phone app (separate product) |
| `phtab` | A.15 | offscope |  |  | Phone app (separate product) |
| `shell` | A.16 | window |  |  | Opens/closes the terminal panel (the prototype's shell answers from sample files). |
| `t-place` | A.16 | window |  |  | Terminal tab switch. |
| `t-sel` | A.16 | window |  |  | Selects a chat in the terminal view. |
| `k10-aside` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-bill` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-branch-people` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-del` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-del-go` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-desk` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-drawer` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-more` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-pair` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-pair-x` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-plan` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-quiet` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `k10-rail` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `ko-approve` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `ko-cancel` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `ko-off` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `ko-off-yes` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `ko-start` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `w-chat` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `w-go` | A.17 | offscope |  |  | keepoak.com web (separate product) |
| `invite` | A.18 | window |  |  | Opens the invite dialog. |
| `p-inv-go` | A.18 | engine | POST /api/profiles | src/collab-server.ts:201; src/collab-server.ts:185; public/people-place.js:389 | 'this' tab: a household profile {name, pin, role adult\|child}. The 'keepoak' tab (email invite to the keepoak.com workspace) has no engine route. First pass wrongly cited settings-kit/apply (people is neverTouched). |
| `p-inv-tab` | A.18 | window |  |  | Switches invite dialog tab. |
| `p-invite` | A.18 | window |  |  | Opens the invite dialog. |
| `p-open-team` | A.18 | window |  |  | Navigates to Team tab. |
| `p-role` | A.18 | engine | POST /api/profiles/{id}/role | src/collab-server.ts:233; public/people-place.js:282 | Adult/Child for a person on this computer. First pass wrongly cited settings-kit/apply. |
| `p-sel` | A.18 | window |  |  | Selects a person. |
| `run-watch` | A.18 | soon |  | src/server.ts:1599 | Team › Live now watches a teammate's task in the keepoak.com workspace. The engine only shows runs owned by the current profile (GET /api/runs/{id} answers 404 otherwise) and has no read-only feed of other people's runs. The first pass's GET /api/runs/{runId} is the OpenAPI text for one's own runs. |
| `share-tab` | A.18 | window |  |  | Switches share dialog tab. |
| `share10` | A.18 | window |  |  | Opens the share dialog. |
| `team-invite` | A.18 | window |  |  | Opens the invite dialog (final prototype maps it to inviteDlg). |
| `team-invite-go` | A.18 | soon |  |  | Invites an email address into the keepoak.com team with seats; the Branch engine has no email invite or seat list (people/sign-in links an email only to an existing profile). In the final prototype team-invite opens inviteDlg, so this button is only in the superseded dialog. First pass wrongly cited settings-kit/apply. |
| `ach-close` | A.19 | window |  |  | Removes the celebration overlay (the engine is told via POST /api/delight/told when it is shown). |
| `achcat` | A.19 | window |  |  | Filters achievements (list from GET /api/delight/achievements). |
| `goal-fill` | A.19 | window |  |  | Puts '/goal ' in the message box. |
| `goal-st` | A.19 | engine | POST /api/sessions/{id}/goal | src/server.ts:1390; src/goal-mode.ts:348; public/goal.js:135 | Body {action: pause\|resume\|stop}; all three variants exist. |
| `guide` | A.19 | window |  |  | Opens the guide menu. |
| `gw-mode` | A.19 | engine | POST /api/never-break | src/never-break/api.ts:61; public/never-break.js:81 | The final handler sets the gateway to off\|when-needed\|on (the Appendix row's text is wrong); saved to the gateway config, effective next start. |
| `gw-prop` | A.19 | engine | POST /api/never-break/proposal/accept; POST /api/never-break/proposal/discard | src/never-break/api.ts:68; src/never-break/api.ts:72; public/never-break.js:95 | Use it / Discard of the gateway's suggested change. First pass wrongly cited settings-kit/apply. |
| `gw-restart` | A.19 | engine | IPC branch:restart (desktop); POST /api/dashboard/restart | src/desktop/restart-ipc.ts:4; src/dashboard-api.ts:193; public/layout.js:1661 | 'Restart the engine' is a real restart; the dashboard route needs the dashboard switched on and the computer's own key. |
| `gwpop` | A.19 | window |  |  | Opens the gateway popover (its contents read GET /api/never-break). |
| `handback` | A.19 | engine | POST /api/runs/{id}/resume; POST /api/linux-desktop/hand-back | src/server.ts:1596; src/server.ts:1587; public/linux-desktop.js:82 | 'Carry on' / 'Hand back' on a step that stopped for the owner resumes that run; 'Hand back' on the agent's computer is the shared Linux desktop's hand-back (that route is only for the Linux desktop Branch drives, owner only). |
| `install` | A.19 | engine | IPC branch:update-install (desktop) | src/desktop/updater-ipc.ts:104; src/desktop/preload.cts:14; public/app.js:1194 | 'Install when nothing is running' is exactly the engine's rule (it waits while tasks are busy). First pass wrongly cited /api/dashboard/restart. |
| `note` | A.19 | proto |  |  | Design-note pins are prototype scaffolding (the design notes panel), not part of the app. |
| `pat` | A.19 | engine | POST /api/delight/noticed | src/delight.ts:202; src/delight.ts:218; public/delight-pet.js:241 | {what:'pat'} counts pats for the pat achievements (only while achievements are on); the pet's words and tip are drawn by the window. |
| `pet` | A.19 | window |  |  | Shows the next tip; tips are window text. |
| `whatsnew13` | A.19 | window |  |  | Opens the What's new dialog, a static list with links into the app. |
