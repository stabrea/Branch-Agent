# Wave 7 task: a coder's toolbox — project maps, language servers, worktrees, checkpoints, OpenAPI tools

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave7/code-ide from the local branch wave2/integration (current tip; it has code tools and file history from wave 2, git tools, `code.patch`/`code.change_set`/`code.check`/`process.*`/`code.run` from orchestration pass 2, the shell with job objects, the tool catalog, workspace search, the plugins loader, and the SDK). Themes: file-and-code-tools (#81), git-integration (#84), codebase-search (#86), ide-and-editor (#92), app-integrations (#85: A0008 language servers, the OpenAPI families). Read those sections in docs/audit/todo.md yourself. Backend only; tiny additive UI in Settings -> Developer. No new dependency: no tree-sitter; language servers and debug adapters are ones the OWNER already has installed (detected by path or configured), spoken to over stdio JSON-RPC written here.

Read first: src/code-tools*.ts, src/code-edit.ts, src/code-change.ts, src/files.ts (WorkspaceFiles.checked, history), src/integrations/git*.ts, src/integrations/github.ts, src/workspace-search*.ts, src/processes.ts, src/integrations/shell*.ts, src/plugins.ts, src/registry.ts, src/catalog.ts, docs/configuration.md code/git sections, tests/code-tools.test.mjs, tests/git.test.mjs, tests/orchestration-2.test.mjs.

Build:
1. Project map (A0537, codebase-indexing family): `code.map` builds and caches a repository map: files, sizes, languages, top-level symbols (functions, classes, exports) per file using small per-language regex scanners for TypeScript/JavaScript, Python, Go, Rust, Java, C#, Markdown headings; imports graph for TS/JS and Python; honours .gitignore and the workspace confinement; incremental on file change; a compact ranked view (files most connected to the words in the request first) that the coder specialist style pre-loads. Say plainly in docs that it is regex-based, not a parser.
2. Language servers (A0008, ide-integrations family): an LSP client over stdio for servers the owner has installed (typescript-language-server, pyright/pylsp, gopls, rust-analyzer; configured executables only, never downloaded), with `code.diagnostics` (errors/warnings for a file or the project), `code.definition`, `code.references`, `code.rename` (as a change set through the approval gate), `code.hover`; server lifecycle under the process manager with the same caps; per-workspace enable switch, off by default.
3. Debug adapter (A0192): a DAP client over stdio for adapters the owner has (node's built-in `--inspect` via the CDP bridge counts; `debugpy` if installed): `debug.start` (launch with breakpoints), `debug.step`, `debug.variables`, `debug.stop`; every launch goes through the same approval as `code.run`; output through the process ring buffers; off by default.
4. Git worktrees and ignore rules (A2333, A0435, A0542): `git.worktree_add/list/remove` confined to a `.branch/worktrees` folder inside the workspace, used by the coder style for risky changes; a `.branchignore` (gitignore syntax) plus .gitignore respected by every read tool and by indexing (knowledge bases and the project map), with a plain refusal naming the rule; "plan branches": a plan or procedure can be tried on a worktree and its diff shown before merging back.
5. Checkpoints, undo and redo (editing-features family, A2059): `workspace.checkpoint` (named snapshot of changed files through the existing history), `workspace.undo` / `workspace.redo` per conversation with a summary of what will change, and a "Restore this point" row in the inspector's timeline; artifacts (A1183): binary outputs (images, zips, builds) stored versioned beside run artifacts with sha256 and a listing tool.
6. Agent export and import (agent-export / portability families): `branch export-agent` writes one zip with specialists, procedures, skills, routing profiles, permission rules and (optionally, redacted) memory; `import-agent` reads it with a manifest shown first and per-section choice; no secrets ever exported.
7. OpenAPI-defined tools (openapi-plugins, openai-compat families, integration-blocks): `tools.from_openapi {url|file, allowlist}` turns an OpenAPI 3 document into registered tools (one per allowed operation, schemas from the document, base URL through the network policy, auth from the locker), filed in the catalog as an external group, descriptions capped and injection-filtered like MCP tools; also `http.request` remains the escape hatch. Notion is the worked example in the docs (against a fake).
8. GitHub actions polish (git-integration families): `github.issues`/`github.checks`/`github.release` read tools and `github.publish_repo` (create a repo and push) behind approval; GitLab read equivalents where the API shape is close; document that a GitHub App is not built.
9. Docs: docs/configuration.md "For coders" section in plain language; docs/design.md note.

Tests (tests/code-ide.test.mjs; fakes for LSP/DAP servers written as tiny Node scripts speaking JSON-RPC over stdio, killed in t.after): map finds symbols across four languages and updates incrementally; ranking prefers connected files; LSP diagnostics/definition/references/rename round-trip against the fake and rename lands as a change set needing approval; DAP launch/step/variables/stop against a fake adapter; worktree add/list/remove confined, plan tried on a worktree and diffed; `.branchignore` refuses a read and skips indexing; checkpoint/undo/redo restore exact bytes; artifact store versions and lists; export/import round trip with no secret; OpenAPI document becomes tools with correct schemas, base URL policy applied, injection-filtered descriptions; GitHub tools against a fake. Existing code-tools/git/orchestration-2/knowledge/catalog-diet tests green; new tools filed in src/catalog.ts.

Acceptance: K1 map; K2 LSP; K3 DAP; K4 worktrees + ignore + plan branches; K5 checkpoints/undo/redo + artifacts; K6 export/import; K7 OpenAPI tools; K8 GitHub polish; D docs; N no new dependency. Report the ids and family rows you consider done. Claim only what the code proves.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; never open a window on the desktop. Every child process in tests is killed in t.after. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
