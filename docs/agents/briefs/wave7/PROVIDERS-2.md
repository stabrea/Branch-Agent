# Wave 7 task: every model service a person might already pay for, as data not code

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave7/providers-2 from the local branch wave2/integration (current tip; it has src/providers.ts (OpenAI, Anthropic, Gemini), provider presets, model plan and routing profiles (wave 7 voice/providers), local models (Ollama/LM Studio), the per-connection probe in src/provider-probe.ts, the pricing table in src/pricing.ts, and OAuth connections). Theme: models.cloud-providers (#56) — the `provider-adapter` family (77 rows) and `model-provider` family in docs/audit/todo.md (read that section yourself), plus A2258/A2367 as honestly scoped. Backend only; Settings -> Models gains preset rows from data, no new screens. No new dependency.

Read first: src/providers.ts, src/provider-presets*.ts, src/models*.ts, src/model-profiles.ts, src/pricing.ts, src/provider-probe.ts, src/local-models.ts, src/network-policy.ts, docs/configuration.md "Models" and "Connections", tests/providers.test.mjs, tests/provider-presets.test.mjs, tests/models.test.mjs, tests/voice-providers.test.mjs.

Build:
1. A provider catalog as data (`data/providers.json`, copied to dist like holidays/suites): for each service the shape (`openai-chat`, `openai-responses`, `anthropic-messages`, `gemini`, `azure-openai`, `bedrock-converse`, `ollama`), base URL, auth style (bearer, `x-api-key`, `api-key` header, query key, AWS SigV4), models list endpoint, which of chat / vision / tools / json-mode / streaming / embeddings / audio / images it offers, default and recommended models, a pricing table pointer, a plain-language note ("needs a project id"), and a sign-up address. Cover at least: OpenAI, Azure OpenAI (deployment URLs + api-version), Anthropic, Google Gemini, Vertex AI (Gemini via OAuth token if feasible; else document), Mistral, Groq, OpenRouter, Together, Fireworks, DeepSeek, xAI, Perplexity, Cohere (chat v2 shape), Cerebras, SambaNova, Hugging Face Inference (OpenAI-compatible route), GitHub Models, Cloudflare Workers AI (OpenAI-compatible route), AWS Bedrock (converse API with SigV4 signed in TypeScript with node:crypto, no SDK), Ollama, LM Studio, vLLM/llama.cpp/LocalAI/Jan (OpenAI-compatible local), Kimi/Moonshot, Zhipu GLM, Qwen (DashScope compatible mode). Every entry has a test that a fake endpoint with that shape completes a chat and, where offered, streams and lists models.
2. Shape adapters: one adapter per SHAPE (not per provider), parameterised by the catalog; the existing OpenAI/Anthropic/Gemini adapters become the first three; add `azure-openai` (deployment path + `api-version` query, `api-key` header), `bedrock-converse` (SigV4 signer, region, model ids; tools and vision mapped; streaming via `converse-stream` event framing), and `cohere-chat-v2`. Everything goes through the network policy guard, keys through the locker, retries through provider-retry, usage through the ledger, prices through pricing.ts with the catalog supplying per-model prices where known (mark unknown as "no price on file", never zero).
3. Capability-aware planning: the model plan refuses to route vision/tools/json-mode to a model that lacks it and says which connection could do it; presets and routing profiles read capabilities from the catalog; `branch doctor --probe` and the Settings card show the probe result per capability.
4. Adding a connection in plain language: `POST /api/connections/from-preset {provider, key, extras}` validates by probing, stores the key in the locker, and returns the models found; an OpenAI-compatible "custom" entry accepts any base URL (loopback or HTTPS only) for services not in the catalog.
5. Fallback and health: per-connection health (last error, latency, rate-limit headers) recorded from real calls; the plan skips a connection that is failing and comes back to it after a cool-down; a "why this model" line already exists — extend it with the fallback reason.
6. Multiple agent runtimes (A2258): honestly scoped — document that Branch runs one runtime and can hand tasks to other agents over A2A/ACP (wave 4); do not build a second runtime. A2367 (PaLM): note it is retired by Google; not built.
7. Docs: docs/configuration.md "Which model services work" table generated from the catalog by a small script (checked into docs, regenerated by `npm run docs:providers`), plain language, with the honest "tested against a fake of the service, not the real one" line, and per-provider setup notes.

Tests (tests/providers-2.test.mjs): catalog validates against a zod schema; every entry completes a chat against a fake with its shape (table-driven), streaming where offered, models list where offered; Azure URL and header construction; Bedrock SigV4 signature matches a known test vector (AWS publishes one) and converse-stream frames decode; Cohere v2 mapping; capability refusal names the alternative; from-preset probes before storing and never logs the key; failing connection is skipped then retried after cool-down; the docs table regenerates deterministically. Existing providers/models/presets/voice-providers/media tests green; pricing tests green.

Acceptance: P1 catalog with ≥25 services, all fake-tested; P2 three new shapes proven (Azure, Bedrock incl. SigV4 vector, Cohere); P3 capability-aware plan; P4 from-preset route; P5 health + fallback; P6 A2258/A2367 honestly documented; D docs table; N no new dependency. Report the family rows and ids you consider done. Claim only what the code proves.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs or start Electron. No real network calls in tests. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
