# Wave 7 task: finish the screens the last waves left as routes, and the observability leftovers

Rules: docs/agents/briefs/wave1/BUILD.md and the design constraints in scratchpad/wave2/REDESIGN.md, REDESIGN-2.md and wave3/SHELL-POLISH.md (tokens only, plain language, KeepOak/ChatGPT-style shell). Branch: wave7/polish-observability from the local branch wave2/integration (current tip; it now has tracing/policy, voice/providers, RAG, collab, web-ui, sdk-misc, catalog-diet, deployment, desktop, cli-tui). Themes: dashboards-and-observability (#58, 58 pieces; read the `## dashboards-and-observability` section of docs/audit/todo.md yourself) plus the specific gaps the integrators handed back, listed below. Front-end heavy with the small routes each item needs. No new dependency.

Read first: public/app.js, shell.js, context-pane.js, approvals.js, usage.js, inspector.js, markdown.js, tracing.js, collab.js, model-profiles.js, i18n.js; src/usage.ts, inspect.ts, tracing*.ts, metrics.ts, policy*.ts, channels/{telegram,discord}.ts, provider-probe.ts, gemini-signin.ts, session-library.ts (labels filter); tests/*-ui.test.mjs, tests/web-ui.test.mjs, tests/static-assets.test.mjs; docs/design.md.

Build, handed-back gaps (each was built as a route or module without its screen or wiring):
1. "What is allowed right now" (tracing item 7): the context pane lists the current conversation's session grants and standing rules that apply, with expiry and a one-click revoke, reading `GET /api/rules/allowed`; the approval card shows the grant it will create.
2. Telegram and Discord approval buttons (tracing item 8): when a run pauses for approval and the conversation is linked to a channel, the question goes out with Yes / Yes always / No buttons (Telegram inline keyboard, Discord components), the answer routes through the same approval endpoint with the exact-bytes fingerprint, and the audit row records the channel; text fallback "reply y / a / n" for WhatsApp/email.
3. Gemini sign-in wiring (voice notes): a "Sign in with Google" button on the Gemini connection card calls `registerSignedInGemini` through a route; `provider-probe.ts` must honour the `bearer` flag instead of putting an OAuth token in `x-goog-api-key`; if Google refuses user tokens for generateContent, the card must say so plainly after the probe and keep the key flow.
4. Rail search and Ctrl+K label filters (collab left-over): labels appear as chips in the Recents search and the command palette, filtering through the existing `labels` search parameter; a label picker on the conversation title menu.
5. Markdown everywhere it was skipped (web-ui note): the Activity screen and the run inspector render Markdown replies with the shared renderer; Appearance's own labels move behind `t(key)` so French covers them.
6. `/model` fallback: when model-profiles.js is not loaded, the composer must still recognise `/model` and `/help` (move slash-command parsing into app.js, keep the module as the handler), and profile cards show preset names not ids.

Build, dashboards-and-observability (pick everything that is real and cheap on top of what exists):
7. Run comparison and statistics (A0296): from Activity, select two runs and see tokens, cost, rounds, tools, duration side by side with the diff of their answers; "statistics" card in Usage: median tokens/round, cost per day, tool success rate, compaction count, from `/api/metrics` and the usage ledger.
8. Trajectory export (A0295/A0458): one JSON per run with messages, tool calls, results (clipped), plan, verdicts, spans, in a documented shape; a "Save trajectory" button in the inspector and `runs.export` tool; also a JSONL export of many runs for evaluation.
9. Live event streaming (A0099): a generic `/api/events/stream` SSE endpoint (authenticated, filtered by kind) that the Activity screen uses for a live feed; document it for the SDK.
10. Usage analytics that a person can read (A0202/A0269/A0346/A0400/A0420/A0459/A0565, the cost family): the Usage screen gets a month view with cost per model, per project, per channel, and a plain sentence forecast ("at this pace, about $X this month"); the CSV export includes cost columns.
11. Metering hooks (A0592): every model call and tool call already writes usage/spans; add a `metering` setting that can export the month's usage as CSV automatically to a workspace folder on a schedule (existing scheduler), nothing outbound.

Tests: extend the UI tests (allowed-list shows a grant and revoke removes it; Telegram button payload shape and answer routing with a fake bot API; Discord component payload; Gemini card wording after a refused probe; label chips filter Recents; Activity renders Markdown; `/model` works with the module absent; run comparison shows two runs; trajectory JSON validates against a small schema; SSE stream delivers filtered events; month view numbers match the ledger; metering export writes the CSV on the scheduled tick). All existing UI/web-ui/shell-ui/static-assets tests green; new public files on the allowlist; classic scripts use `var $`; tokens only; screenshots (Forest/Daylight, 1280 and 400) in C:/Users/bishi/AppData/Local/Temp/claude-session-files/wave7-polish/.

Acceptance: G1–G6 handed-back gaps proven; D1 comparison + statistics; D2 trajectory export; D3 event stream; D4 usage month view + forecast + CSV cost; D5 metering export; S screenshots; N no new dependency. Report audit ids you consider done from dashboards-and-observability and from the handed-back items (A1805/A2075 channel approvals, A0051 Gemini sign-in if it truly works). Claim only what the code proves.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs or start Electron. New tool names must map to a toolbox in src/catalog.ts. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
