# Wave 7 task: a voice you can talk to, and models you can switch on the fly

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave7/voice-providers from the local branch wave2/integration (current tip; it has wave-1 voice (browser recording, /api/voice/transcribe, /api/voice/speak through an OpenAI-compatible audio endpoint), wave-5 media (audio transcription tool), local-models, the model plan/routing in src/models*.ts, providers.ts, channels). Themes: voice-io (#65, 21 pieces), models.cloud-providers (#56, 11 pieces), models.routing-and-cost (#79). Backend first; UI additive in the composer (mic/speaker), Settings -> Models and Settings -> Voice. No new dependency.

Read first: public/voice.js, src/voice*.ts, src/server.ts voice routes, src/media*.ts, src/providers.ts (audio()/images()), src/models.ts and src/model-plan*.ts (plan(), presets), src/local-models.ts, src/channels/* (Telegram voice notes arrive as files), src/usage.ts (cost), docs/configuration.md voice and models sections, tests/voice.test.mjs, tests/media.test.mjs, tests/models.test.mjs.

Build, voice:
1. Speech-to-text everywhere (A0328/A0654/A1893/A2067): one `Transcription` service with adapters for OpenAI-compatible `/audio/transcriptions` (Whisper shape), Gemini (inline audio), and a local route through whisper.cpp or faster-whisper IF the owner has one installed and points the app at it (detect a configured executable; never download models yourself). Voice notes arriving on Telegram/Discord/WhatsApp are transcribed and handled as text with the transcript shown quoted; the composer's hold-to-talk uses the same service; language auto-detect with an override; cost recorded.
2. Text-to-speech (A1995/A1894/A2107/A2266): `Speech` service with OpenAI-compatible `/audio/speech`, Gemini TTS, and a local route through Windows' built-in `System.Speech`/SAPI voices via a hidden PowerShell child (`windowsHide: true`, no console) so TTS works offline with no key; a `voice.say` tool and a "Read this aloud" button on every reply; the channels can send an audio reply when the person sent a voice note (setting, default off).
3. Voice conversation mode (A0970/A1001/A2162): a "Talk" toggle in the composer: hold-to-talk -> transcribe -> run -> speak the answer, with a live status row, interruption by pressing again, and the transcript kept in the conversation like typed text. Realtime API sessions (A1212/A2293): implement a `RealtimeSession` over the OpenAI Realtime WebSocket ONLY if it fits within the existing network policy and no dependency; otherwise document it as a follow-up and say so. No always-on wake word.
4. Voice settings: pick STT and TTS provider/voice/speed, test buttons, a plain sentence about where audio is sent and the cost per minute, "keep audio on this computer" preference that forces local routes or refuses.

Build, providers and routing:
5. Runtime model switching (A0735): `/model` in the composer and `models.switch` tool switch the model for the current conversation without restart; the rail shows the active model; costs still per model.
6. Routing profiles (A0576, A1012): named profiles ("cheap and fast", "best quality", "private", "long context") mapping task kinds (chat, plan, code, vision, voice, embeddings, summarise) to provider+model with fallbacks; a profile is data (JSON in settings), the presets from local-models become profiles; a "why this model" line in the run inspector.
7. Sign in with Google for Gemini (A0051): use the wave-4 OAuth connections (PKCE) to obtain a Gemini API access token where Google allows it; if the Gemini API does not accept OAuth user tokens for generateContent, say so in docs and keep the key flow. Do not build Google account sign-in for anything else.
8. Provider diagnostics: `branch doctor` and Settings -> Models show, per connection, a live probe result (auth ok, models listed, audio/images/embeddings supported) using the existing doctor plumbing.

Tests (tests/voice-providers.test.mjs; fakes only, no real audio devices or network): STT adapters parse the three shapes and record cost; a fake channel voice note becomes a transcript run; TTS OpenAI shape returns audio bytes and the local SAPI route is shape-tested (do not play sound; the PowerShell script string is asserted, and if you run it, run with -File to a temp wav with no console); voice.say tool respects "keep audio on this computer"; talk mode state machine (idle -> listening -> thinking -> speaking -> idle, interrupt) unit-tested; model switch changes the plan for one conversation only; routing profile picks fallbacks in order and the inspector line names the reason; Google OAuth token used as bearer for Gemini only when configured. Existing voice/media/models/providers tests green.

Acceptance: V1 STT proven across three adapters + channels; V2 TTS proven incl. offline Windows voice; V3 talk mode proven; V4 voice settings + privacy preference; M1 runtime switch; M2 routing profiles; M3 Google sign-in honestly scoped; M4 diagnostics; D docs; N no new dependency. Report audit ids done (candidates: A1995 A0328 A0654 A0888 A0970 A1001 A1212 A1893 A1894 A2067 A2068 A2107 A2162 A2173 A2266 A2293 A0051 A0576 A0735 A1012 A0167 A0498 A1139). Claim only what the code proves.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs or start Electron. Never play audio or open a microphone on the owner's computer in tests. Do not remove the worktree when done.
