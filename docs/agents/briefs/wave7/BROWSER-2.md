# Wave 7 task: browser automation, second pass — uploads, pictures, annotations, your real signed-in browser, resilient actions

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave7/browser-2 from the local branch wave2/integration (current tip; it has the wave-3 browser (Playwright-driven, "Sign in once" persistent context, screenshots with password fields blacked out, upload/download, tables/cards extraction, PDF save, tabs, action and site caps, approval gating), the wave-5 desktop screen control, the wave-7 resource rules (host matchers for browser actions), tracing spans, and the media/vision path). Theme: browser-automation (#62, 26 pieces). Read the theme's section in docs/audit/todo.md yourself. Backend first; small additive UI in the existing browser card. No new dependency (Playwright is already present).

Read first: src/browser*.ts, src/integrations/browser*.ts, tests/browser*.test.mjs, src/policy-resources.ts, src/tracing.ts, src/media*.ts (vision), src/desktop*.ts (for the shared refusal list of password windows), docs/configuration.md browser section.

Build (skip anything the existing code already proves; say so in the report):
1. Gaps named by the audit: file-upload control (A1639) with workspace-confined paths; screenshot output the model can look at (A2129) with the existing redaction, plus element-level screenshots; DOM annotation directives and session state (A2144, A2286): numbered overlays on interactive elements (the "set of marks" approach) drawn in the page, a compact text map of them, and stable ids that survive re-renders within a session; structured extraction with the model's help (extraction family): `browser.extract {schema}` returning validated JSON from the page.
2. Your real browser (A2183, A2241, browser-persistence): connect to the owner's already-running Chrome/Edge over CDP when the owner starts it with a debugging port from a button in Settings ("Let Branch use my browser for this task"), so signed-in sessions are reused without ever seeing a password; strictly opt-in per task, clearly shown in the context pane, closed at the end, and refused for password-manager and banking hosts by the same list the desktop control uses; document the risk plainly.
3. Self-healing actions (resilience family): when a selector fails, retry by accessible role/name, by visible text, then by the annotation id, and record which worked in the run's trace; never more than N attempts, and never a different site.
4. Domain skills (A1568): three shipped browser skill packages using the wave-4 skill format: "search the web and summarise the top results", "fill a form from a document", "watch a page for a change" (reuse monitors), each with tests against local fixture pages.
5. Trace capture (trace-capture family): optional Playwright trace zip saved as a run artifact when the owner turns on "keep a browser recording for this task", scrubbed of the token and never containing password fields (they are blacked out before capture; assert it).
6. Hybrid browser + desktop (hybrid-tooling, computer-use abstraction): one `computer.*` facade that routes to browser tools when the target is a page and to desktop tools when it is a window, with the same approval and caps; keep both underlying tool sets.
7. Web search and fetch providers (web-access family): pluggable search backends behind the existing `web.search` (SearXNG self-hosted, Brave, Tavily, Exa, Serper via keys in the locker; a DuckDuckGo HTML fallback with clear rate-limit notes) chosen in settings; remote browsers (Browserbase/cloud) documented as not built.
8. Docs: docs/configuration.md browser section rewritten in plain language with the new switches and the honest limits.

Tests (tests/browser-2.test.mjs, against local fixture pages served by the test; no real sites): upload from workspace succeeds and outside refuses; element screenshot redacts a password field; annotations number elements and stay stable after a re-render; extract returns schema-valid JSON and refuses off-schema; CDP attach against a headless Chromium launched by the test with a debugging port (never the owner's real browser in tests), reused cookies visible, banking host refused, closed at end; self-healing recovers a renamed selector and stops after N; the three skills complete against fixtures; trace zip exists, contains no token and no password value; computer.* routes to the right layer; search backends parse each provider's shape from fakes. Existing browser/browser-more/approvals/tracing-policy tests green; new tools filed in src/catalog.ts.

Acceptance: B1 audit gaps; B2 real-browser opt-in with refusals; B3 self-healing; B4 three skills; B5 trace capture proven safe; B6 computer facade; B7 search backends; D docs; N no new dependency. Report ids and family rows you consider done. Claim only what the code proves.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; never attach to or open the owner's real browser; headless Chromium only, and never open a visible window on the desktop. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
