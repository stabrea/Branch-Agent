# Wave 7 task: traces you can export, and permission rules you can read

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave7/tracing-policy from the local branch wave2/integration (head 04c7312 or later; it has the wave-2 cost/trace export, the wave-6 audit table and approval kinds, the run inspector, and the secrets scrubber). Themes: tracing-and-telemetry (#63, 40 pieces) and permissions-and-policies (#66, 33 pieces). Backend first; small additive UI in Settings -> Approvals and Usage only. No new dependency: OTLP is plain JSON over HTTP; nothing is sent anywhere unless the owner turns it on.

Read first: src/usage.ts (getRunTimeline, trace export), src/activity.ts, src/receipts*.ts, src/audit.ts, src/policy.ts, src/tool-categories.ts, src/runtime.ts (events, askApproval, hideSecrets), src/network-policy.ts, src/inspect.ts, docs/configuration.md sections on usage, approvals and diagnostics, tests/cost-trace.test.mjs, tests/approvals.test.mjs, tests/sdk-misc.test.mjs.

Build, tracing:
1. Spans (`src/tracing.ts`): a tiny tracer (trace id per run, span per model round, tool call, retrieval, channel delivery, child run; parent links; attributes = the same numbers the inspector shows, scrubbed through `hideSecrets`), stored in a `spans` table beside events, with W3C `traceparent` propagated into outbound webhooks/A2A/ACP calls and read from inbound ones so a trace crosses agents.
2. Export (`src/tracing-export.ts`): OTLP/HTTP JSON (`/v1/traces`, and `/v1/metrics` for the usage counters) to an owner-typed endpoint with optional headers (stored in the locker as `secret://` references), batched, retried, through the network policy; adapters that speak the same span model to Langfuse (ingestion API) and LangSmith (runs API), HTTP JSON only, no SDKs; a "Send a test trace" button; export is OFF by default and every export is written to the audit table.
3. Metrics and health: a `/api/metrics` route in Prometheus text format (runs, tokens, cost, tool latency histograms, queue depth, compaction count) behind the app token; the Usage screen gains a "Health" card reading it.
4. Local telemetry policy (A0155/A0385/A1418): there is NO product telemetry and never will be by default; write that plainly in docs; the existing diagnostics bundle gains the last N spans, scrubbed.
5. Error reports (A0566): uncaught errors in the server and the desktop main process are recorded as spans with the stack scrubbed; an opt-in "send crash reports to my own endpoint" that reuses the OTLP exporter; nothing to Sentry.

Build, permissions and policies:
6. Rules with resources (A0262/A2006/A0048): extend the policy rule model with an optional resource matcher (path glob inside the workspace, host pattern for web/browser, channel id, command alias) and an outcome allow/ask/deny; evaluation order: exact tool+resource, then tool kind, then default; keep every existing rule valid (no matcher = any resource). Show rules as sentences in Settings -> Approvals ("Ask before writing files under /finance", "Never browse example.com") with add/remove and a "test a decision" box.
7. Session grants (A0220/A2088): "Yes for this conversation" already exists; make it a first-class grant with expiry shown in the approval card and a "What is allowed right now" list in the context pane; grants end when the conversation ends or on "Lock".
8. Approval protocol (A1805/A2075): approvals over the WebSocket for phones/channels: the pending question, its exact target (sanitised bytes shown; A1686: the decision is bound to the hash of the exact argument bytes so a changed command needs a new yes), answer routes, and the audit row; the Telegram/Discord channels get the same y/n/always buttons.
9. Auth hardening (A2076): rate-limit failed token/PIN/pairing attempts per source with a plain message; audit row on lockout.
10. Browser action approval policy (A1521): browser click/type/submit/download go through the same rules with host resources (reuse, do not duplicate the browser's own caps).
11. Docs: docs/configuration.md "Traces and metrics" and "Permission rules" in plain language; docs/design.md note for the rule sentences.

Tests (tests/tracing-policy.test.mjs): spans nest correctly for a scripted run with one tool call and one child run; traceparent propagates out and in (fake A2A peer); OTLP JSON shape validated against a small schema and the fake collector receives batched, retried payloads with headers from the locker never in logs; Langfuse/LangSmith adapters produce the documented shapes; /api/metrics parses as Prometheus text; export off by default and audited when on; resource rules: path/host/command matchers decide as documented, precedence proven, legacy rules unchanged; session grant expires with the conversation; exact-bytes binding rejects a changed command; rate limit locks after N failures and audits; browser action passes through host rules. Existing approvals/cost-trace/sdk-misc/guardrails tests green.

Acceptance: T1 spans + propagation proven; T2 OTLP/Langfuse/LangSmith export proven against fakes, off by default, audited; T3 metrics proven; T4 telemetry statement in docs; P1 resource rules proven with UI sentences; P2 session grants proven; P3 exact-bytes approval over the socket proven; P4 auth rate limit proven; P5 browser actions under rules; D docs; N no new dependency. Report audit ids you consider done (candidates: A0031 A0835 A0972 A0597 A0613 A0823 A0056 A0155 A0225 A0270 A0385 A0566 A0882 A0955 A1127 A1189 A1263 A1303 A1356 A1418 A1805 A0048 A0152 A0220 A0245 A0262 A0398 A0596 A0617 A0796 A0824 A1126 A1521 A1629 A1685 A1686 A1834 A2006 A2027 A2053 A2075 A2076 A2088 A2276 A2296). Claim only what the code proves.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs or start Electron. Do not remove the worktree when done (a forced remove deletes through the junction).
