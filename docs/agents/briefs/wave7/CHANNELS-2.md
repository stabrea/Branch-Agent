# Wave 7 task: reach people where they already are — more chat services, as data where possible

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave7/channels-2 from the local branch wave2/integration (current tip: Telegram, Discord, Slack, WhatsApp Cloud API and email from waves 1 and 3 in src/channels/*, with pairing, allowlists, mention rules, retries, delivery ledger with quiet hours, voice notes, approval buttons on Telegram/Discord, the network policy guard, and webhooks/triggers). Theme: messaging-channels (#59, the remaining rows: A0115 A2014 A2034 A2066 A2091 A2117 A2156 A2238 A2255 A2295 A2349) and webhooks-and-triggers (#83) leftovers. Backend first; the existing Connections cards gain rows from data. No new dependency.

Read first: src/channels/router.ts, src/channels/telegram.ts, discord.ts, slack.ts, whatsapp.ts, email*.ts, deliveries.ts, src/channels/adapter*.ts (the adapter contract), src/webhooks.ts, src/triggers*.ts, src/network-policy.ts, tests/channels*.test.mjs, docs/configuration.md Connections section.

Build:
1. Adapter contract and a data-driven "simple webhook bot" shape: many team-chat services share one pattern (incoming webhook URL to send, outgoing webhook/HTTP callback to receive, shared secret or signature). Implement one `WebhookChatAdapter` parameterised by a catalog entry (`data/channels.json`: name, send URL template, message JSON shape, receive signature scheme, mention format, max message length, chunking, file/voice support flags, docs link) and cover with it: Mattermost, Rocket.Chat, Google Chat (spaces webhooks + Chat API events), Microsoft Teams (incoming webhook for send + outgoing webhook HMAC for receive; document that a full Bot Framework app is not built), Zulip, Matrix (client-server API with an access token: sync long-poll + send; end-to-end encrypted rooms documented as unsupported), Feishu/Lark (bot webhook + event subscription with verification token), DingTalk (custom robot with sign + outgoing), WeCom (group robot), LINE Messaging API (reply/push + signature), Viber (bot API), Signal via `signal-cli` ONLY if the owner has it installed (JSON-RPC over stdio, detected by path). Each: pairing code on first contact, allowlist, "reply when mentioned", chunking to the service limit, retries via the delivery ledger, quiet hours, approval buttons or the "reply y / a / n" fallback, voice notes where the service offers audio (else documented), and the same token-scrubbing tests as Telegram.
2. Social channels (A2066): document honestly which are feasible without a partner programme: X/Twitter DMs and Instagram/Facebook Messenger need app review; implement Messenger/Instagram via the same Meta Graph webhook + send shape as WhatsApp Cloud API (shared code), flagged "needs Meta app review", and leave X out with a sentence why.
3. Service connectors (A2349, A2091 extensions): a `channels.connectors` registry so a plugin can register a channel adapter (extend the wave-4 plugin loader like provider plugins), with a tiny example plugin in the tests; and `channels.broadcast` (send one message to several linked chats, respecting quiet hours) plus `channels.digest` (the morning brief goes out over a chosen channel).
4. Webhooks and triggers polish (#83): outbound webhook templates per event with a JSON-shape editor in Settings (data only), HMAC signature header with a per-hook secret from the locker, replay protection on inbound triggers (timestamp + nonce window), a "test this hook" button, and a delivery log view; document the event catalogue.
5. Docs: docs/configuration.md "Connections" rewritten as a table generated from data/channels.json (what each service can do: text, files, voice in/out, buttons, mentions; what it needs from the owner) with plain setup notes; README bullet.

Tests (tests/channels-2.test.mjs, fake servers for every service shape): send/receive/signature-verify/chunk/pairing/allowlist/mention for each catalog entry (table-driven); a wrong signature is refused and audited; tokens never appear in events or errors; Matrix sync loop reconnects with backoff and stops on close; signal-cli path refused when not installed; Messenger path shares WhatsApp code and is flagged; plugin adapter registers and delivers; broadcast honours quiet hours; digest delivers the brief; outbound webhook signature verifies, replay refused, test button posts; docs table regenerates byte-identically. Existing channels/channels-more/triggers-webhooks/deliveries tests green; new tools filed in src/catalog.ts.

Acceptance: C1 catalog-driven adapters (≥10 services proven against fakes); C2 social honestly scoped with Messenger/Instagram built; C3 connectors registry + broadcast + digest; C4 webhooks polish; D docs table; N no new dependency. Report ids you consider done. Claim only what the code proves; a service counts only when send, receive, signature and pairing are tested against a fake of its real shape.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; never open a window on the desktop; no real network calls. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
