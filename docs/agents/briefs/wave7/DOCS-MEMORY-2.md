# Wave 7 task: read every document people actually have, and memory that knows what kind of thing it remembers

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave7/docs-memory-2 from the local branch wave2/integration (current tip: knowledge bases with embeddings/BM25/chunking, memory retrieval with hybrid search and nightly consolidation proposals, documents library, memory hygiene from wave 3, household profiles, tracing). Themes: documents-and-rag leftovers (#57: A1869 document ingestion and parsing, A1667 structured insertion from conversations, A1545 document-assisted analysis, doc-processing family) and memory-features (#74: read the `## memory-features` section of docs/audit/todo.md yourself). Backend first; small additive UI in Documents and Memory. No new dependency: Office files are zips of XML (node:zlib + a small XML walker), PDF text comes from a pure-TypeScript reader for the common cases, with honest limits.

Read first: src/documents*.ts, src/document-readers*.ts (whatever reads files today), src/chunking.ts, src/knowledge-bases.ts, src/memory*.ts, src/memory-retrieval.ts, src/memory-consolidate.ts, src/memory-hygiene*.ts (wave 3), src/store.ts memory tables, tests/documents.test.mjs, tests/memory*.test.mjs, tests/rag-vector.test.mjs, docs/configuration.md documents and memory sections.

Build, documents:
1. Readers (A1869, doc-processing): DOCX (paragraphs, headings, tables, footnotes; styles → heading levels), XLSX (sheets → CSV-like rows, formulas' cached values), PPTX (slide titles and text, notes), ODT/ODS basics, EPUB (chapters), RTF (text), plain PDF text extraction in pure TS: parse xref/objects, inflate FlateDecode streams, walk content streams for Tj/TJ with ToUnicode CMaps and standard encodings, reconstruct lines by y position, per-page output; refuse encrypted PDFs plainly; scanned PDFs (no text) report "this PDF is pictures; ask me to read it with a vision model" and hand the pages to the existing media/vision path when the owner allows. Every reader is size-capped and time-bounded (run in the existing worker or a child with the job limits) and never follows external references inside the file.
2. Ingestion into knowledge bases: chunking uses headings/slides/sheets/pages as boundaries with page/slide/sheet citations; reindex detects changed files by hash; a "What could not be read" list per collection with the reason.
3. Document-assisted analysis (A1545): `documents.analyse {file, question}` picks the reader, extracts tables as data.* tables when present (reuse the wave-5 data tools), and answers with citations; a "Compare two documents" tool producing a plain-language diff of sections.
4. Structured insertion from conversations (A1667): after a conversation, a review pass (existing governance/memory review) can propose knowledge-base entries (a fact card with title, body, source turn, confidence) into a chosen collection; proposals only, owner accepts in the Memory screen; accepted cards are indexed like documents.

Build, memory:
5. Kinds and layers (A2035 three-layer, A0422 role memory, A1247 task-centric): every fact gets a `kind` (preference, fact-about-person, fact-about-world, procedure-hint, project-note, task-scratch) and a `layer` (working = this conversation, task = this task's scratch, long-term); task-scratch is dropped when the task ends unless promoted; the injection into context takes from layers in a documented order and budget; role memory: facts can be scoped to a specialist (already partly there) and to a project.
6. Cleanup as a procedure the owner can run (A2187, A2245): a shipped procedure "Tidy my memory" that runs the hygiene checks (duplicates, contradictions, stale, never-used), shows the proposals in one screen, and a "memory health" line in the diagnostics bundle with counts; an evaluation script (`branch eval memory`) that measures retrieval quality on a small labelled set stored in data/ (owner-editable), reporting hit rate before and after consolidation.
7. Pluggable storage, honestly (A1425, A1475): define a `MemoryBackend` interface around today's SQLite store (read/write/search/list/forget), make the SQLite implementation the only one, and document the contract for others; add a file-based export/import that round-trips everything including kinds and layers (extend the existing memory export).
8. Persistent sessions (A0148): confirm what exists (sessions persist already) and make "continue this conversation on the phone" real by exposing the session list and last messages over the paired remote listener with the same auth; if remote access is not on staging in a usable form, document and skip.
9. Docs: docs/configuration.md "Reading documents" (what formats, what cannot be read, what is sent where) and "How memory is organised" in plain language.

Tests (tests/docs-memory-2.test.mjs, with fixture files you generate in the test: build DOCX/XLSX/PPTX zips from XML strings; craft a small uncompressed and a FlateDecode PDF by hand): each reader extracts expected text and structure; encrypted PDF refused; scanned PDF reports pictures; size/time caps; chunk citations carry page/slide/sheet; reindex by hash; "could not be read" list; analyse answers with citation and tables; compare produces a diff; conversation proposals created and accepted into a collection; kinds/layers assigned, task-scratch dropped and promoted, injection order and budget; tidy procedure proposes and deletes nothing; eval script reports hit rate; export/import round trip. Existing documents/memory/rag-vector/knowledge tests green; new tools filed in src/catalog.ts.

Acceptance: R1 readers; R2 ingestion citations; R3 analyse + compare; R4 conversation proposals; M1 kinds/layers; M2 tidy + eval; M3 backend interface + export; M4 sessions on the phone honestly scoped; D docs; N no new dependency. Report ids and family rows you consider done. Claim only what the code proves.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; never open a window on the desktop. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
