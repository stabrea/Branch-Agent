# Wave 7 task: orchestration, second pass (specialist styles, patches, background processes, flows, skills that improve themselves)

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave7/orchestration-2 from the local branch wave2/integration (current tip; it has wave-3 plans/steering/reviewers/fan-out, wave-6 durable workflows (`workflows` table, step kinds), the catalog with tool groups and `tools.expand`, code tools and git from wave 2, the shell with job objects, skills/plugins from wave 4, and skill governance drafts). Themes: agent-orchestration (#55, the remaining families), skills-and-recipes (#78), plugin-and-extension-system (#70) leftovers. Backend first; UI additive in Specialists, Procedures and Skills only. No new dependency.

Read first: src/runtime.ts (plan/act loop, delegation, fan-out, follow-ups), src/orchestration*.ts, src/workflows*.ts (wave 6, if merged; otherwise the wave6/collab-workflows branch), src/code-tools*.ts, src/integrations/shell*.ts, src/skills*.ts, src/skill-governance*.ts, src/plugins.ts, src/catalog.ts, docs/CHECKPOINT.md orchestration sections, tests/delegation.test.mjs, tests/orchestration*.test.mjs, tests/code-tools.test.mjs.

Build:
1. Specialist styles (agent-specialist family: planner, ReAct, subtask-aware, critic, researcher, coder): a specialist declares a `style` that changes its loop: `react` (think/act/observe with a visible scratch line), `plan-execute` (existing plan runner), `critic` (review-only, no writing tools), `researcher` (web/knowledge groups pre-opened, citations required), `coder` (code+git groups, patch flow below). Styles are data on the specialist record; the owner picks one in Specialists; the run inspector shows the style.
2. Patch application and multi-file edits (file-editing family, workflow-hooks): a `code.patch` tool taking a unified diff, applying it atomically inside the workspace with a dry-run preview, per-file backup through the existing file history, refusal outside the workspace and on binary files; a `code.change_set` that groups edits across files with one approval ("Change 7 files?") showing a summary; a post-apply hook that runs the project's configured check command (tests/lint) and feeds the result back into the loop; PR opening reuses the wave-2/6 GitHub tool.
3. Background and long-running processes (process-management family): `process.start/list/read/stop` for commands that outlive a tool call (dev servers, watchers) under the shell's job object and the same limits, with output ring buffers, a per-run cap, automatic stop when the conversation is closed or the app quits, and a Specialists/Activity row showing what is running; approvals as for shell.
4. Deferred tool calls and toolsets (tool-deferral, tool-discovery): a tool call can return `{ deferred: true, id }` and complete later through the follow-up mechanism (long uploads, human tasks); `tools.search {query}` finds tools by words across closed catalog groups (reuse src/catalog.ts scoring) so the model can discover a tool without opening a whole group.
5. Flows as an API (flow-crud, flow-execution, graph-execution, callback-system): expose the wave-6 workflows as `GET/POST/PUT/DELETE /api/flows` with a JSON graph (nodes = steps, edges = branch outcomes), `POST /api/flows/:id/run`, run status with per-node state, and webhook callbacks on node completion through the existing webhook deliveries; a simple read-only graph view (SVG boxes and arrows, tokens only) in Procedures. If workflows are not on staging when you start, build against the branch and note it.
6. Code execution tool (code-execution family): `code.run {language: "javascript"|"python", source}` executing in a fresh child process (Node for JS; the owner's configured Python if present) under the shell limits and job object, workspace-confined cwd, no network unless allowed, output clipped; presented as "run a small script" with the same approval as shell.
7. Skills that improve themselves, safely (A0608/A1827/A2112/A2341): after a run that used a skill, the existing governance drafts a revision; add a diff view, a one-click accept/reject, an automatic "try the revised skill on the last three tasks in dry-run and compare" check before it is offered, and export/import of skills as files plus `skills.sync` with a git folder the owner names. Plugin marketplace (A plugin-marketplace family): a local catalog file listing plugins with signatures/hashes, install from a folder or zip with the manifest shown first; no remote marketplace.
8. Docs: docs/configuration.md sections in plain language for each item; README bullets.

Tests (tests/orchestration-2.test.mjs): each style changes the loop as described (scripted provider); react scratch lines appear in events and not in the final answer; code.patch applies/rejects/dry-runs and restores on failure; change_set needs one approval and lists files; post-apply check result reaches the next round; process.start outlives the tool call, is listed, read, stopped, and is killed on conversation close and shell close; deferred call completes via follow-up; tools.search finds a closed tool; flows CRUD + run + node states + callback delivery (fake webhook receiver); code.run executes JS with limits and refuses when not allowed; skill revision dry-run comparison and accept/reject; plugin install from a folder shows the manifest and verifies the hash. Existing delegation/orchestration/code-tools/shell/skills tests green.

Acceptance: O1 styles; O2 patch + change set + hook; O3 background processes; O4 deferred + search; O5 flows API + graph view; O6 code.run; O7 skill self-improvement with dry-run check + plugin local catalog; D docs; N no new dependency. Report the audit ids you consider done from the agent-orchestration, skills-and-recipes and plugin-and-extension-system sections of docs/audit/todo.md. Claim only what the code proves.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs or start Electron. Every child process you start in tests must be killed in t.after. Do not remove the worktree when done.
