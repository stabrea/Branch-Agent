# Wave 8 task: design QA — every screen, both themes, two widths, plain words everywhere

Rules: docs/agents/briefs/wave1/BUILD.md and the design constraints in scratchpad/wave2/REDESIGN.md, REDESIGN-2.md and wave3/SHELL-POLISH.md (KeepOak tokens only, ChatGPT/Codex-style shell: rail with Sections/Projects/Recents, single message column, floating composer, right context pane, copper accent, acorn). Branch: wave8/design-qa from the local branch wave2/integration (current tip). This is a quality pass, not a feature task. Front-end only (public/*.js, *.css, index.html, locales) plus docs/design.md. No new dependency.

The owner is not technical and wants the app to look and read like a finished product: "a mix of the ChatGPT app and Codex, keeping the KeepOak design". Seven waves of builders added cards and screens quickly; they do not all match.

Read first: public/shell.css, style.css, web-ui.css, every public/*.js that renders a settings card or section, public/locales/en.json, docs/design.md, tests/*-ui.test.mjs, tests/web-ui.test.mjs, the screenshot scripts in tests/wave7-screenshots.mjs and scratchpad/walkthrough.mjs, and the reference captures in C:/Users/bishi/AppData/Local/Temp/claude-session-files/design-refs/chatgpt-01-main.png and chatgpt-02-switcher.png (the owner's ChatGPT/Codex reference; only those two files are valid).

Do, in this order:
1. Inventory: start the web server with the demo provider (see tests for `startServer` with `BRANCH_PROVIDER=demo` or the walkthrough script) and, with Playwright headless (no visible window), screenshot EVERY section and every settings card at 1280x800 and 400x800 in Forest and Daylight into C:/Users/bishi/AppData/Local/Temp/claude-session-files/wave8-design-qa/before/. List every screen in a table in your report.
2. Fix, screen by screen (keep each change small and additive): (a) hard-coded colours, ad-hoc paddings/font sizes/radii replaced by tokens; (b) every card follows one card anatomy (title, one-line purpose, controls, a plain note, a single primary button), same spacing, same button styles; (c) empty states say what the screen is for and offer one action; (d) error and success notes read as plain sentences (no codes, no jargon such as "endpoint", "payload", "SSE", "OAuth", "MCP" without a plain gloss on first use per screen); (e) 400 px: no horizontal scroll anywhere, controls wrap, the rail folds, tables get their own scroll; (f) keyboard: focus rings visible on every control, Escape closes every popover, Tab order sane; (g) reduce-motion honoured; (h) all remaining English strings in the sections behind `t(key)` (finish the wave-6 localisation mechanically; fr.json updated as a draft); (i) consistency of names across the app (one name per concept: "task" not "run" in owner-facing text; "connection" for a model service; "toolbox"; check docs/design.md's glossary or write one).
3. First-run and everyday flows: walk the first-run welcome → connect a model → first task → look inside → settings, at both widths, and fix anything confusing; make sure the update banner, the lock screen, the practice workspace and the household-profile switch all read plainly.
4. After screenshots into .../after/ with the same names; a before/after contact sheet (one HTML file, tokens only) in that folder.
5. Tests: extend tests/shell-ui.test.mjs and tests/web-ui.test.mjs with: no element wider than the viewport at 400 px on every section; no hard-coded colour in any public/*.css (grep in the test, allow the manifest/icons exception); every visible `button`/`input` has an accessible name; Escape closes each popover you touched; the glossary words appear consistently (a small assertion over the rendered text of each section). All existing UI tests green.
6. docs/design.md: the card anatomy, the glossary, the token list, and the screenshot procedure.

Acceptance: Q1 inventory table with before/after screenshots for every screen; Q2 zero hard-coded colours outside the documented exception; Q3 400 px clean on every section (test); Q4 plain-language pass with the glossary applied (test); Q5 focus/Escape/reduce-motion (test); Q6 localisation finished for the sections; Q7 docs; N no new dependency. Report under 60 lines with the table and the paths of the contact sheet.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Playwright headless only; never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; never open a visible window. Before your final commit, merge the current `wave2/integration` into your branch and re-run the UI tests. Do not remove the worktree when done.
