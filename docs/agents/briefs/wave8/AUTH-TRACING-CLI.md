# Wave 8 task: the remaining secrets/auth, tracing and CLI rows — build the real ones, verify the already-true ones, document the rest

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave8/auth-tracing-cli from the local branch wave2/integration (current tip: `branch` CLI with a terminal view, daemon mode, paired remote listener, secrets vault with `secret://` references and (on integrate/reopened, landing soon) Bitwarden/1Password CLI references, tracing with OTLP/Langfuse/LangSmith export, audit table, hardening pass 2). Themes: secrets-and-auth (#69), tracing-and-telemetry (#63), cli-and-tui (#72): read the open (unticked) rows in each issue with `gh issue view <n>` and the matching docs/audit/todo.md sections. Backend + CLI. No new dependency.

Per open row decide BUILD / VERIFY (it is already true on this tree: name the file and the test, and if no test asserts it, add one) / DOCUMENT (why not, or how an existing feature covers it) / NOT APPLICABLE (single-owner Windows desktop assistant). Guidance:

CLI (#72): A0284, A0383, A0425, A0551, A0723, A0793, A1191, A0488 are "a CLI exists" rows from other projects: VERIFY against `src/cli.ts` + tests/cli*.test.mjs and add one test that lists every command from `cliCommands` and runs `--help` for each. A1159 npm-distributed CLI: add a `bin` entry (`branch`) to package.json pointing at dist/cli.js, a `scripts/pack-cli.mjs` that produces a tarball with `npm pack` (no publish), and a docs section on installing it locally with `npm install -g ./branch-agent-x.y.z.tgz`; test the packed tarball runs `branch --help`. A0306 persistent server and multi-client attach: `branch chat --attach` connects a second terminal to the running daemon's conversation (list, pick, stream) over the local API; test with two clients. A0012: add `--session`, `--resume`, `--fork` to `branch run`. A0910 cron API client: `branch schedule add/list/remove` over the API. A2103 CLI model adapters: a provider shape `cli-agent` that runs an installed coding agent CLI as a model backend (Claude Code `claude -p`, Codex `codex exec`, GitHub Copilot CLI) with prompt on stdin/args, JSON output when the tool offers it, stripped env, job limits, no network of its own beyond what the tool does; catalog rows flagged "uses your installed tool and its own sign-in"; tests with fake executables. A2404 interactive terminals: VERIFY the wave-4 terminal view; document. A0137 local web client: VERIFY (the web app) and document.

Secrets and auth (#69): A0100 session API keys: short-lived scoped tokens (`branch token create --scope read --minutes 60`) for the SDK/widget/extension, revocable, audited, never the master session key; A1930 same feature (temporary tokens) — one implementation, two rows, say so. A0221 command-based secret expansion: `secret://cmd/<name>` where the command is a configured executable (never free text), same rules as the Bitwarden path. A0686 generic sender allowlist: one allowlist model across every channel (already per channel; make it one shape + UI). A0614 session auth + pairing: VERIFY. A1003/A1004/A1804 pluggable/composite/gateway authentication: for the remote listener, support token, pairing code, and a per-device certificate pin as a composable chain (config), documented; OIDC stays N/A. A1807 secret providers: a `SecretProvider` interface over locker / env / file / Bitwarden / 1Password / command, documented as the contract. A2177 profile authentication: VERIFY the household PIN + lockout. A2003, A2095: NOT APPLICABLE (single owner; document). A1897, A2074, A2216: already N/A.

Tracing (#63): A1149, A0856, A1498, A1583, A0799, A1287: the OTLP exporter and spans exist; VERIFY as one implementation and add an "instrumentation coverage" test asserting spans for runs, rounds, tools, retrievals, channel deliveries and child runs; document the projection. A0056: add the OTLP logs signal (events → log records) behind the same switch. A1440 trace ids and Langfuse binding: put the trace id in the run inspector and in Langfuse export as the trace id (already?) and add a `branch trace <run>` command printing the id and export status. A1497 LangSmith: VERIFY. A1523 LLM tracing and usage telemetry: VERIFY (spans + usage ledger). A1931 event audit coverage: extend the audit table writers to cover exports, profile switches, lockdown, connections added/removed, borrowed browser start/stop; test each. A0800 logs API: `GET /api/logs` (JSONL, filtered, authenticated) as the local "logs API"; Grafana/Loki documented as "point your collector at OTLP". A1620, A1751, A0681, A1334: NOT APPLICABLE or covered; document.

Tests: tests/auth-tracing-cli.test.mjs plus updates; whole non-desktop suite green after your final merge of `wave2/integration`; catalog width under its literal; new tools filed in src/catalog.ts. Fake executables only for CLI agents and command secrets; never call the owner's real tools.

Acceptance: per open row in #69, #63, #72 one line: built (test) / verified (file + test) / documented (anchor) / not applicable (reason). Report under 50 lines with branch + head SHA and test counts.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; no window; no real network. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
