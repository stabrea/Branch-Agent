# Wave 8 task: hardening pass 2 — the gaps reviewers named after wave 7

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave8/hardening-2 from the local branch wave2/integration (current tip: everything through hardening pass 1, channels-2, evaluation-2). Not a feature task: each item is a specific gap an integrator documented as "not fixed". Keep each fix small and tested; skip (and say why) anything that turns out large. No new dependency.

Items:
1. Embeddings through the network policy. `EmbeddingClient` / `embeddingsFor(connection, call = globalThis.fetch)`, `KnowledgeBases.embeddings()`, document and knowledge search, and tool meaning search all call providers with a bare fetch; the rest of the provider layer uses `policy.guard(fetch)` / `assertProviderEndpoint`. Thread the guard through every embedding path (one constructor argument, default to the guarded fetch from the app), add a test that a disallowed host is refused for embeddings, and make the provider layer's convention explicit in docs (which calls go through the policy and why the loopback/HTTPS-only check is the floor).
2. Several pending approvals per session. `Approvals.pending` is `Map<sessionId, one question>`, so a second concurrent ask on one conversation or MCP connection replaces the first. Make pending a small ordered list per session keyed by fingerprint (cap 8, oldest refused with a plain sentence), keep every existing answer route working (answer by fingerprint, fall back to "the oldest" when only one), lift the MCP one-per-connection cap accordingly, and prove with tests for the web UI path, the channel button path and the MCP path.
3. Unguessable webhook ids. `/webhooks/chat/<id>` and `/webhooks/whatsapp/<id>` use the owner-chosen channel id in the path. Add a per-channel random path secret (≥128 bits) appended to the route (`/webhooks/chat/<id>/<secret>`), shown once in the Connections card with a "copy address" button and rotatable; old-shape routes answer 404 after a one-release grace period behind a setting defaulting to "accept both" with a dated note. Update docs and the generated channels table.
4. Studies and the shared execution limit. The study pool runs up to 4 cells under one HTTP place. Make each cell take a place from `ExecutionLimit` with `onRoom` cooperation and prove no deadlock when several studies run (each run reuses its own place; a study waits rather than starves); attribute rubric judge cost to the study cell and to the `budget` scorer; confine `study.source.directory` to the workspace or a configured benchmarks folder.
5. Per-run teardown for language servers and debug adapters (coder toolbox): a debuggee or server started by a run stops when that run ends unless the owner pinned it (setting), matching `process.*`'s conversation-close behaviour; test with the fake adapters.
6. OpenAPI services persist. `tools.from_openapi` registrations vanish on restart; store the non-secret registration (source, allowlist, base URL) in settings and rebuild at startup, with auth still from the locker; `tools.forget_service` removes it; test the restart.
7. Refused hosts for the borrowed browser are additive-only owner-editable: `extraRefusedHosts` in settings, never a way to remove the built-in list; UI row; test.
8. Audit `source` for channel answers: extend the `auditSources` enum with `telegram`, `discord`, `slack`, `whatsapp`, `email`, `chat` and record the channel in `source` while keeping the task's origin in a new `origin` column; migrate existing rows (origin = old source); update the CSV export and the filter; tests.
9. Approval fingerprint everywhere: verify that every approval entry point (web UI card, `branch approve`, channel buttons, MCP held call, workflows resume, flows resume) binds the answer to the exact-bytes fingerprint; add tests where missing; document the one rule in docs/configuration.md.
10. Study/eval and tool-evaluation side effects: `branch eval tools` writes a real workspace file and memory notes; run those in the practice workspace (wave 6) or a temp project instead, and prove the owner's memory is untouched.

Tests: tests/hardening-2.test.mjs plus updates to the suites each item touches. Whole non-desktop suite green after your final merge of `wave2/integration`. Catalog width check must stay under its literal.

Acceptance: one line per item: done / skipped (why). Report under 40 lines with branch + head SHA and test counts.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; never open a window on the desktop; no real network. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
