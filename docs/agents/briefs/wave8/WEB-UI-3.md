# Wave 8 task: web UI, third pass — artifacts, flow editor, charts, reports, an Obsidian bridge, and the rest of the dashboards list

Rules: docs/agents/briefs/wave1/BUILD.md and the design constraints in scratchpad/wave2/REDESIGN.md, REDESIGN-2.md, wave3/SHELL-POLISH.md and docs/design.md (card anatomy, glossary, tokens only; the wave-8 design-QA tests now enforce 400 px width, accessible names and no hard-coded colours, so run tests/shell-ui.test.mjs and tests/web-ui.test.mjs often). Branch: wave8/web-ui-3 from the local branch wave2/integration (current tip). Themes: web-ui (#64) and dashboards-and-observability (#58), the rows still open; read both issues (`gh issue view 64`, `gh issue view 58`) and the matching sections of docs/audit/todo.md. Front-end heavy plus the routes each item needs. No new dependency.

Decide per open row: BUILD, DOCUMENT (how an existing feature covers it), or NOT APPLICABLE (framework-specific demos such as Gradio, Streamlit, Chainlit, Next.js, a Rust TUI, a desktop pet: say so in one line each in docs/configuration.md "What Branch is not"). Build these:
1. Interactive artifacts (A1997, A2240, A0080): when a reply contains a fenced `html`, `svg`, or a `chart` block, show it as an artifact card beside the message: rendered in a sandboxed iframe (CSP `sandbox` without `allow-same-origin`, no network, tokens passed in as CSS variables), with "open larger", "copy code", "save to workspace"; code artifacts (`javascript`, `python`) get a "run a small script" button that goes through `code.run` and its switch; artifacts are saved as run artifacts and listed in the Documents section. Reuse the MCP apps sandbox route.
2. Charts in replies (A1454): the wave-5 `data.chart` SVG and any `chart` block render inline with hover values and a data table toggle; export as PNG through a canvas in the page (no server rendering).
3. Visual flow editor (A0275, A0777, A0584, A0593): the wave-7 flow view becomes editable: add/remove nodes (step kinds from the workflows model), connect outcomes, edit a node's fields in a side form generated from the step schema, save through `PUT /api/flows/:id`; a run timeline under the graph shows each node's state and duration live over the run WebSocket; a cron/interval picker for schedules (A2093) with a plain-language preview ("every weekday at 9").
4. Reports in several formats (A1490, A1730): "Save as report" on any task, study or knowledge answer producing Markdown, a self-contained HTML page (reuse the share-page renderer with redaction) and a print-ready view for PDF (browser print to PDF, no server PDF); episode HTML export of a run's trajectory with the same renderer.
5. Persistent to-do tracking (A0635): a `todos` table and `todos.add/list/done` tools, a To-do card in the context pane fed by the model's plan steps and by the owner, with due dates handed to schedules; plain, small.
6. Obsidian bridge (A2133): the owner uses Obsidian. Build `obsidian.sync` (and a setting): write memory facts, knowledge cards, saved reports and conversation exports as Markdown notes into a vault folder the owner names (confined, front-matter with ids, never overwriting a note the owner edited: detect by hash and write a `.branch-conflict.md` beside it), and read back `#branch` tagged notes as documents for search. No Obsidian plugin code; this is a folder bridge and the docs say so.
7. Embeddable widget and browser extension (A1934, A1932): a tiny `public/widget.js` that a local page can include to talk to Branch over the paired remote listener only (never the loopback token), documented as for the owner's own pages; a minimal Manifest V3 extension folder under `extras/browser-extension/` with a popup that sends the current page URL/selection to Branch as a task through the same paired listener, unsigned, with install steps; both behind a setting, off by default.
8. Dashboards leftovers: structured logs view (A0752: filter the event log by kind/run/time, export JSONL), request-limit accounting (A1209: requests per minute per connection with the provider's rate-limit headers shown), cache-aware cost lines (A1441: cached answers listed with what they would have cost), watches and alerts (A0647/A0656: the existing monitors gain an "alert me" action through channels and an opt-in screenshot watch that uses screen control when the owner has it on, diffing a region and reporting a change; off by default), multi-assistant conversations (A1940: choose a specialist per message from the composer, shown as the message's author), multilingual UI (A1983: finish the remaining `t(key)` coverage for field labels, buttons and notes; fr.json real translations for the 62 English values the QA integrator flagged).
9. Docs: docs/configuration.md sections for artifacts, reports, to-dos, the Obsidian bridge, the widget and extension; docs/design.md notes for the artifact card and flow editor.

Tests: extend the UI suites (artifact renders in a sandboxed frame, script cannot reach the parent or the network, run button goes through the switch; chart hover and table toggle; flow editor adds a node and saves, timeline updates over the socket; reports produced in three formats with redaction; to-dos round trip and hand a due date to schedules; Obsidian sync writes front-matter notes, refuses outside the vault, never overwrites an edited note, reads tagged notes back; widget refuses the loopback token; extension folder validates as Manifest V3; logs filter and export; request-limit and cache lines appear; alert action delivers through a fake channel; specialist-per-message author shown; locale coverage test tightened to include labels and buttons). All existing UI/design-QA tests green; new public files on the allowlist and in tests/static-assets.test.mjs; new tools filed in src/catalog.ts with the width check intact.

Acceptance: per open row in #64 and #58 one line: built (test) / documented (anchor) / not applicable (reason). Screenshots (Forest/Daylight, 1280 and 400) of the artifact card, flow editor, report view and to-do card in C:/Users/bishi/AppData/Local/Temp/claude-session-files/wave8-web-ui-3/. Report under 60 lines with branch + head SHA and test counts.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Playwright headless only; never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; never open a visible window; never touch the owner's real Obsidian vault (temp folders only). Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
