# Wave 8 task: documents and memory, third pass — writing Office files, a light knowledge graph, summaries, pictures as documents, retention

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave8/docs-3 from the local branch wave2/integration (current tip: readers for Office/PDF/EPUB, knowledge bases with hybrid search and citations, memory kinds/layers/tidy, knowledge cards, `data.*` tables, the vision path in media, the Obsidian bridge if wave8/web-ui-3 has landed). Themes: documents-and-rag (#57) and memory-features (#74) open rows; read them with `gh issue view 57` and `gh issue view 74`. Backend first; small additive UI in Documents and Memory. No new dependency (Office files are zips of XML: write them the same way they are read).

Build, per open row (BUILD / VERIFY with file + test / DOCUMENT / NOT APPLICABLE):
1. Office document creation and editing (A2145, A2263): `documents.write` producing DOCX (headings, paragraphs, lists, tables, simple styles), XLSX (sheets from `data.*` tables or rows, number formats, a formula cell kept as text with cached value), PPTX (title + bullet slides, an image slide from a run artifact), and Markdown/HTML; `documents.edit` for DOCX/XLSX: replace text, append a section, update a table, add a sheet, preserving everything else in the package byte-for-byte except the parts touched; results are workspace files behind the write permission. "Live cowork" scoped honestly: no real-time co-editing; document.
2. Pictures as documents (A0946): image files in a knowledge base are described once by a vision model when the owner allows it (cost shown, cached by hash), the description indexed with a citation to the picture; OCR-like text from screenshots through the same path; refused plainly when no vision connection exists.
3. Knowledge-base summaries and management (A1668, A1867, A2362, A2036): `knowledge.summarise` (map-reduce over chunks with citations, cached per collection version), collection management (rename, merge, split by folder, export/import as a zip of Markdown + metadata, size and cost figures), a "Refresh from new conversations" action that turns accepted knowledge cards into the collection (self-evolving, proposals first).
4. A light knowledge graph (A0994): entities and relations extracted from chunks into `kb_entities`/`kb_relations` (model-extracted when allowed, else noun-phrase co-occurrence), `knowledge.graph` returning the neighbourhood of an entity with citations, used as one more retriever behind the wave-6 interface (graph hop → chunks), and a small read-only SVG in Documents; honest limits in docs.
5. Storage and examples (A1013, A1144, A1426, A2343, A2264): VERIFY what exists (MemoryBackend/Retriever contracts, the PDF reader) and add docs/handbook-style worked examples of RAG in docs/configuration.md; RAGFlow and other external services NOT APPLICABLE (document).
6. Memory rows (#74): A0650 automatic local long-term memory + RAG: VERIFY (memory review + knowledge bases) with one end-to-end test that a conversation fact is later retrieved with the knowledge base as a source; A1926/A1989 extraction and injection: VERIFY and add the missing test if any; A2185 layered filesystem memory: a Markdown mirror of long-term memory in the workspace (`memory/` folder, one note per fact kind, regenerated, read-only by the model, useful with the Obsidian bridge); A1117 in-memory document store: an ephemeral per-task store for pasted text (indexed for the task only, dropped at the end); A2168 QMD: NOT APPLICABLE (document); A1425/A1475 already documented.
7. Retention and pruning for documents and knowledge: age/size limits per collection with proposals first (coordinate with the sandbox-remote branch's conversation retention setting: same shape, do not duplicate the settings model; if it has not landed, define the minimal shape and note it).
8. Docs: docs/configuration.md sections in plain language; docs/handbook chapter 03 updated if the handbook branch has landed (else leave a note for it).

Tests (tests/docs-3.test.mjs): DOCX/XLSX/PPTX written and read back by the existing readers with the expected structure; edit preserves untouched parts byte-for-byte; picture description indexed with citation and cached, refused without vision; summarise cites and caches; management operations; refresh from cards; graph extraction, neighbourhood query, retriever hop; memory mirror regenerated and read-only; ephemeral store dropped at task end; retention proposes and exports before deleting. Existing documents/knowledge/memory/rag-vector/docs-memory-2 tests green; catalog width intact; new tools filed in src/catalog.ts.

Acceptance: per open row in #57 and #74 one line: built (test) / verified / documented / not applicable. Report under 50 lines with branch + head SHA and test counts.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; no window; no real network; temp folders only. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
