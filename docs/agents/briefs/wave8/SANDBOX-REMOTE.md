# Wave 8 task: real sandboxes where the computer offers one, SSH workspaces, and the last policy rows

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave8/sandbox-remote from the local branch wave2/integration (current tip: job objects + netless for host commands, `code.run`, `process.*`, sandbox choice per rule (wave 8 re-opened branch: `src/sandbox.ts`, landing shortly; if not yet on staging, build against `integrate/reopened`), the borrowed browser, the network policy, the paired remote listener, git tools and worktrees). Themes: sandboxing-and-isolation (#80), mobile-and-remote-access (#87), permissions-and-policies (#66) open rows; read them with `gh issue view <n>`. Backend only; small additive UI in Settings -> Approvals and a Remote card. No new dependency: use what Windows already has (OpenSSH client `ssh.exe`/`scp.exe`/`sftp.exe`, Windows Sandbox `.wsb` when the feature is enabled, Docker Desktop or Podman when installed, WSL when installed), detected by path, never installed.

Build:
1. Sandbox backends (A2131, A2152, A2160, A2277, A0797, A1520): a `SandboxBackend` interface (`prepare(workspaceSlice) -> handle`, `run(command, limits) -> result`, `collect(paths)`, `dispose()`), with backends: `job-object` (today's behaviour, always available), `docker` (`docker run --rm --network none --memory --cpus --read-only` with the workspace slice mounted read-write under `/work`, image from settings, only when `docker` or `podman` is on disk and answers `version`), `wsl` (run inside a named WSL distro with the slice copied in and back; when `wsl.exe` exists), `windows-sandbox` (generate a `.wsb` that maps the slice read-write, runs a bounded logon command and collects results; only when the optional feature is enabled; document that it opens a window, so it is never the default and requires the owner to choose it). The per-rule sandbox choice (from the re-opened branch) selects a backend; `code.run` and `process.start` honour it; a backend that is unavailable yields a plain refusal naming what to install. Code-block validation before execution (A1520): a fast static check (forbidden APIs list per language, size cap, no network imports unless the rule allows) that runs before any backend.
2. Filesystem and firewall policies (A0018, A1618, A1413): per-rule `paths` (which workspace folders a sandboxed command may see: the slice), an explicit "firewall" card that shows the effective network policy as sentences ("Scripts cannot reach the internet", "Browser may visit these sites") with a test button; allowed-domain navigation restrictions verified for the browser and shown in the same card.
3. Per-session rate limiting (A2028): fixed-window limits per conversation and per channel sender (requests per minute, tokens per hour), configurable, with the queue used for the owner and discard-with-a-sentence for strangers; audited.
4. Reusable authenticated browser state (A1770): a named browser profile per project ("Sign in once" already persists a context): make profiles selectable and exportable/importable as an encrypted bundle through the locker key; document that the borrowed browser is the alternative.
5. SSH workspaces (A0329, A0648, A2279, A0544, A1582): a `remote` workspace kind backed by the owner's OpenSSH client: `remote.add` (host alias from the owner's ssh config only, never passwords), `files.*` and `shell.execute` routed over `ssh`/`sftp` with the same confinement rules applied to the remote root, host key trust taken from the owner's known_hosts (refuse unknown hosts, never `StrictHostKeyChecking=no`), per-remote allowlist of executables, all traffic through the child process (no network policy bypass: document that ssh is the policy boundary and show the host in approvals); git-aware apply (A0544): before applying a change set on any workspace, create a git stash or checkpoint commit on a `branch/` ref when the folder is a git repo, and offer "undo to before" from the inspector; git workspaces (A1582): a project may point at a git branch/worktree and switching projects switches it.
6. Policy rows (#66): A0365 authentication and access policy: VERIFY against the household roles from the re-opened branch and the remote listener's pairing; A2027 generic allowlist: one allowlist shape across channels and the remote listener (coordinate: the auth-tracing-cli branch is building a generic sender allowlist for channels; reuse its shape if it has landed, else build the minimal shape and note it); A2233 manifest resource permissions: plugin and skill manifests declare the permissions, paths and hosts they need, shown before enabling, enforced by narrowing the tool set (verify what wave 4 did; complete the enforcement half); A2344 conversation pruning retention policy: a setting to prune conversations older than N days or larger than N MB, proposals first, audited, with export offered before deletion; A1005 RBAC/FGA integrations: NOT APPLICABLE (document).
7. Mobile companions (A1777, A2080): NOT APPLICABLE beyond the PWA + pairing already shipped; document.
8. Docs: docs/configuration.md "Sandboxes" (which backend does what, what each protects against, what it does not), "Remote computers over SSH", "Firewall", "Retention"; plain language.

Tests (tests/sandbox-remote.test.mjs, fakes only): backend selection by rule; unavailable backend refuses plainly; docker backend builds the exact argv against a fake `docker` on PATH (never a real container); wsl and windows-sandbox generate the expected files/argv against fakes; static code check refuses a forbidden API and a network import; paths slice enforced; firewall card sentences match the policy; per-session limits queue the owner and discard a stranger with a sentence; browser profile export/import round trip encrypted; ssh workspace: fake `ssh`/`sftp` scripts prove host alias only, unknown host refused, confinement on remote paths, executable allowlist, approval shows the host; git-aware apply creates the checkpoint and undo restores; manifest permissions narrow the tool set; retention proposes, exports, then deletes only on the owner's yes. Whole non-desktop suite green after the final merge; catalog width intact; new tools filed in src/catalog.ts.

Acceptance: per open row in #80, #87, #66 one line: built (test) / verified (file + test) / documented (anchor) / not applicable (reason). Report under 50 lines with branch + head SHA and test counts.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; never start a real container, WSL distro, Windows Sandbox, or ssh connection in tests (fake executables only); no window; no real network. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
