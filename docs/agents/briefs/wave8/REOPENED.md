# Wave 8 task: finish the rows the verifier re-opened

Rules: docs/agents/briefs/wave1/BUILD.md. Branch: wave8/reopened from the local branch wave2/integration (current tip). Read docs/audit/verification-2026-09-17.md first: it lists 24 audit ids that were ticked on a builder's claim and re-opened because the code was partial or missing. Your job is to finish the ones that fit a local Windows desktop assistant and document the rest honestly. Backend first, small additive UI. No new dependency.

Do, per id (the verifier's reason is in the report; read the code it names):
- A1519 Bitwarden / 1Password credential resolution: a `secret://bitwarden/<item>` and `secret://1password/<item>` reference kind resolved through the owner's already-installed CLI (`bw` via the existing session pattern the owner uses, `op read`) at the call boundary only; values never stored, never logged, scrubbed; a plain refusal when the CLI is absent or locked; setting off by default; tests with a fake CLI on PATH. (The owner uses Bitwarden; treat this as real.)
- A0245 sandbox policy selection: a per-tool `sandbox` choice (job object + netless, job object only, none) selectable in the approval rules and shown in the approval card; `code.run`/`process.*`/shell honour it; test.
- A1629 unmatched commands fail closed: a host command with no matching allow/ask/deny rule is asked, never silently allowed; make it the default and test; document the migration (existing owners see one more question the first time).
- A1465 OS permission checks: before screen control or the microphone, ask Windows whether the app has the permission (registry keys under `CapabilityAccessManager\ConsentStore` for microphone/camera; for UI Automation, a probe call) and show a plain sentence with the Settings deep link (`ms-settings:privacy-microphone`) when it is missing; test the decision logic with fakes.
- A2006 role and resource permissions: household profiles get named roles (owner, adult, child) with grants (which tool kinds, which projects, spending cap) enforced in `checkPolicy` and shown in the profile card; tests for a child role refused a purchase-class tool and a project outside its grant.
- A0824 / A1126 human-in-the-loop hooks: a lifecycle hook may now BLOCK (return `{ decision: "ask" | "deny", reason }`) instead of notify-only, with a timeout and audit; test a hook that turns an allow into an ask.
- A0638 token figures in the terminal view: `branch chat` status line already has tokens? verify; add per-answer tokens and money in the transcript view; test.
- A1481 replay in the inspector: "Replay this task" re-runs the same prompt with the same tools available and shows the two side by side (reuse compare); test.
- A0195 / A0317 / A0372 / A0405 / A1093 / A1278 (orchestration patterns): implement the missing patterns as specialist styles or fan-out modes over the existing plan/fan-out engine: `supervisor` (one specialist routes sub-tasks to others and merges), `handoff` (a specialist transfers the conversation to another with a reason, visible in the transcript), `swarm` (parallel workers on a shared list with claim/release), `router` (classify then dispatch); each with a test and a plain doc sentence; if any is a stretch, document it as not built rather than half-building.
- A2277 and A1841: read the verifier's reason and do the small missing piece or document.
- A2043 (Windows only), A2162 (wake word), A1103 (vector connectors), A0566 (Sentry), A1897 (inbound OIDC), A2074 (WebAuthn), A2216 (invite-only accounts): DOCUMENT as not applicable for a single-owner Windows desktop assistant (with one sentence each in docs/configuration.md "What Branch is not") and say so in the report; do not build.

Tests: tests/reopened.test.mjs plus updates to touched suites; whole non-desktop suite green after your final merge of `wave2/integration`; catalog width under its literal; new tools filed in src/catalog.ts.

Acceptance: one line per id: built (test name) / documented (doc anchor). Report under 40 lines with branch + head SHA and test counts.

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron; never call the owner's real `bw`/`op` (fakes only); no real network. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
