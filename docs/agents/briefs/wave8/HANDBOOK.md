# Wave 8 task: the owner's handbook — one set of documents a non-technical person can actually use

Rules: docs/agents/briefs/wave1/BUILD.md (plain language above all). Branch: wave8/handbook from the local branch wave2/integration (current tip). Documentation and in-app help only: no behaviour changes to src/ except the tiny help route described below. No new dependency.

Why: eight waves appended sections to docs/configuration.md, README.md and docs/CHECKPOINT.md. They are accurate but enormous, ordered by when things were built rather than by what a person wants to do, and full of batch numbers. The owner is not technical and wants the app to feel finished.

Read first: README.md, docs/configuration.md (all of it), docs/design.md, docs/CHECKPOINT.md (skim), docs/api.md, docs/audit/README.md, public/locales/en.json (the words the UI uses), the glossary in docs/design.md, and the release notes in the GitHub releases (`gh release list`, `gh release view vX --json body`).

Build:
1. `docs/handbook/` — a task-ordered handbook, one Markdown file per chapter, each starting with "What this is for" and "In one minute" and written for someone who has never used a terminal: 00-start-here (install, first task, what the screen shows), 01-connect-a-model (services, keys, local models, cost), 02-everyday-tasks (asking, files, documents, pictures, voice, talk mode), 03-your-memory-and-knowledge (memory, knowledge bases, tidy, what leaves the computer), 04-permissions-and-safety (approvals, rules, lockdown, secrets, lock, household profiles, what Branch will never do), 05-reach-it-anywhere (chat apps, phone, background running), 06-automate (schedules, watches, workflows, flows, procedures, skills), 07-for-builders (CLI, SDK, API, MCP, plugins, OpenAPI tools, benchmarks), 08-troubleshooting (doctor, diagnostics bundle, updates and the previous copy, common messages and what they mean), 09-glossary (from docs/design.md, expanded), 10-what-branch-is-not (the honest list of non-goals, from the verification reports and docs). Every chapter links to the exact settings screen ("Settings → Models") using the names the UI uses (check en.json). No batch numbers, no wave numbers, no audit ids.
2. docs/configuration.md becomes the reference: keep every setting, route and switch, but reorganise into the same ten chapters with an index at the top, delete duplicate paragraphs, and move history notes into docs/CHECKPOINT.md. A script `scripts/check-docs.mjs` fails if any setting key in src/*Schema definitions (grep `z.object` in settings/config files) is missing from the reference, and if any handbook link points at a non-existent chapter or anchor; run it in `npm test` via the existing test glob (a small test file that spawns the script).
3. README.md: a two-screen front page: what Branch is (one paragraph), a screenshot (use an existing one from claude-session-files/wave8-design-qa/after/ copied into docs/images/, 1280 Forest conversation), install in three steps, the ten chapter links, contributing and licence. Keep the THIRD_PARTY_NOTICES reference.
4. In-app help: a `GET /api/help/<chapter>` route that serves the handbook chapters (bundled into dist by scripts/copy-data.mjs like the other data files) and a "Help" entry in the owner menu that opens the relevant chapter for the current section in the context pane through the existing Markdown renderer; the palette gets "Help: <chapter>" entries. Small, tokens only, plain language; test that every chapter is served and that the help entry opens the chapter for the Documents section.
5. Release notes template: docs/release-notes-template.md with the plain-language structure used for 0.11–0.15, and a `scripts/release-notes.mjs` that drafts the next notes from CHECKPOINT sections added since the last tag (a starting point, not final prose).
6. Check every claim in the handbook against the code while writing (the two verification reports list what is partial or not built); where a feature is partial, say what works and what does not in one sentence.

Tests: tests/handbook.test.mjs (all chapters exist with the two opening headings; links resolve; check-docs passes; help route serves every chapter; the help entry opens the right chapter). Existing UI tests green.

Acceptance: H1 ten chapters written for a non-technical reader; H2 reference reorganised with the checker passing; H3 README front page; H4 in-app help; H5 notes template + drafter; H6 claims checked; N no new dependency. Report under 40 lines with branch + head SHA, word counts per chapter, and the three claims you found to be untrue while writing (or say none).

Housekeeping: your worktree has no node_modules; create a junction with PowerShell `New-Item -ItemType Junction -Path <worktree>/node_modules -Target C:/Users/bishi/Documents/Codex/Branch-build/node_modules`. Playwright headless only; never run tests/desktop*.test.mjs, tests/screen-control.test.mjs, or start Electron. Before your final commit, merge the current `wave2/integration` into your branch and re-run your tests. Do not remove the worktree when done.
