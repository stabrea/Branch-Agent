# Wave 6 task: the web app grows up (PWA, inspector, playground, live intervention, languages)

Rules: docs/agents/briefs/wave1/BUILD.md and the design constraints in wave2/REDESIGN.md, REDESIGN-2.md and wave3/SHELL-POLISH.md (tokens only, no hard-coded colours, plain language). Branch: wave6/web-ui from the local branch wave2/integration. Themes: web-ui (#64), the display items in dashboards-and-observability (#58), and from "other" (#60): A0482 markdown/code rendering, A0447 trajectory inspector, A0437/A0483 localisation. Front-end heavy; keep shell.css/shell.js edits additive and small.

Read: public/app.js, public/shell.js, public/context-pane.js, public/usage.js (timeline), src/usage.ts (getRunTimeline), src/streams.ts, src/ws.ts, src/server.ts static allowlist, tests/*-ui.test.mjs, docs/design.md.

Build:
1. Markdown and code rendering done properly (A0482): a small dependency-free renderer already exists or is partial — make headings, lists, tables, links (opened externally through the desktop allowlist), inline code and fenced code blocks with a copy button and language label render consistently in messages, memory and documents; sanitise (no raw HTML); tests against a fixture document.
2. Run inspector (A0285/A0447/A0295): a "Look inside" view for any run (from the context pane receipts, Activity, or a message's "Worked with N tools" row) showing the timeline with durations, each model round (prompt size, tokens, cost), each tool call with clipped input/output and its receipt status, plan steps, critic verdicts, steering notes; export the run as JSON. Uses `GET /api/runs/:id/timeline` and receipts; add `GET /api/runs/:id/inspect` if a single call is cleaner.
3. Live intervention (A1302, user-intervention family): while a run is working, the message column shows a live status row (current step, elapsed, tokens so far) with Pause / Steer (text box → `POST /api/runs/:id/steer`) / Stop, and approval questions appear inline as a card with Yes once / Yes for this conversation / Always / No, wired to the policy routes; streamed over the existing WebSocket.
4. Context and token display (token-display family, A0057): a quiet meter in the composer area showing context used vs. budget for the current conversation and the estimated cost so far; a per-session stats popover.
5. Playground (A0731): a "Try a tool" screen under Sections → Settings → Developer: pick any tool, fill its schema-generated form, run it under the normal permission gate, see the raw result; and a "Try a prompt against two models" pane reusing the evaluation compare route when present.
6. PWA (A1904): web manifest, icons, a service worker that caches the shell files for the phone-access case (no offline runs; a clear offline banner), install prompt suppressed inside Electron. Verify at 400 px.
7. Localisation groundwork (A0437/A0483): all UI strings in public/*.js and index.html move behind a tiny `t(key)` with `public/locales/en.json` as the source of truth and a second locale file (`fr.json` machine-drafted, marked as draft) proving the mechanism; language choice in Appearance; dates/numbers via Intl. Do this last and keep the diff mechanical.

Tests: extend the UI tests: markdown fixture renders expected structure and strips script; inspector shows a scripted run's tool rows; live row appears during a slow scripted run and Stop cancels; approval card answers reach the policy route; token meter updates; playground runs a read tool; manifest and service worker served (static-assets test) and registration skipped under `?desktop=1`; locale switch changes a visible label. All existing UI tests green.

Acceptance: U1 renderer proven and sanitised; U2 inspector proven; U3 live intervention proven; U4 meter proven; U5 playground proven; U6 PWA served and gated; U7 localisation mechanism proven; U8 screenshots (Forest/Daylight, 1280 and 400) in C:/Users/bishi/AppData/Local/Temp/claude-session-files/wave6-web-ui/; U9 no new dependency. Report the ids you consider done.
