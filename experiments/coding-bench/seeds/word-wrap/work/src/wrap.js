/**
 * Wraps text so that no line is longer than `width` characters.
 *
 * - Words are separated by single spaces; break only at spaces.
 * - A word longer than `width` goes on a line of its own, unbroken.
 * - Lines are joined with "\n" and have no trailing spaces.
 * - Existing "\n" characters in the input start a new line.
 */
export function wrap(text, width) {
  throw new Error("not implemented");
}
