# A sample document

This is a plain paragraph with **bold**, *italic* and `branch start` inside it,
plus a [link to the docs](https://example.com/docs) that should open outside the app.

## What it covers

- a plain bullet
- a bullet with **bold** inside
- a bullet with `code` inside

1. first step
2. second step

| Thing | What it does |
| --- | --- |
| `files.read` | reads one file |
| `files.write` | writes one file |

> A quoted line, for when something is worth setting apart.

### One more level

```javascript
const answer = 42;
console.log(answer);
```

Nothing below this line may ever become markup:

<script>window.__markdownEscaped = true;</script>
<img src="x" onerror="window.__markdownEscaped = true">
[click me](javascript:alert(1))

---

The end.
